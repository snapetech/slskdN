---
category: security
audience: operators
area: web-toolchain
action: none
breaking: false
---
The Web development toolchain now uses a TypeScript version supported by its ESLint parser, declares flat-config dependencies directly, and removes vulnerable unused lint packages; clean installs, lint, and npm audit are green.
