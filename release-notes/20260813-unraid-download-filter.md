---
category: fixed
audience: users, operators
area: packaging
action: none
breaking: false
---
The Unraid template now exposes `SLSKD_DOWNLOAD_FILTER_EXCLUDE` as a semicolon-separated environment override, so container users can configure the global outbound download exclusion policy without editing the YAML file.
