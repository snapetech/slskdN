# Soulseek.NET Web Example

This React example uses Vite and is served by the `examples/Web/api` project after a production build.

## Scripts

Run the development server:

```bash
npm install
npm start
```

Build static assets into `build/`:

```bash
npm ci
npm run build
```

Preview the production build locally:

```bash
npm run preview
```
