# Bug Burn-Down Ledger

This ledger is the canonical intake queue for bug council findings. It is for defects only: feature ideas and broad refactors stay in `memory-bank/tasks.md` until a concrete bug is accepted.

## Operating Rules

- Add one row per distinct finding and keep the `ID` stable after triage.
- Do not fix directly from scanner output. First classify confidence, duplicate status, and regression requirement.
- Stop-ship and High rows need a repro, failing check, focused test proposal, or static proof before implementation.
- Every accepted bug fix must add or update a regression check before it can move to `Fixed`.
- Repeated or non-obvious gotchas must be documented in `memory-bank/decisions/adr-0001-known-gotchas.md` before continuing with implementation.
- Release-worthy user, security, packaging, or ops fixes need a `docs/CHANGELOG.md` entry.
- Record validation commands in the row before moving a bug to `Verified`.

## Domains

- Backend/Security
- Frontend/Workflow
- Release/Ops
- Network Health
- Tests/Tooling
- Docs/Config

## Severity

- `Stop-ship`: direct security exposure, file/data loss, or release artifact that must not ship.
- `High`: core workflow breakage, meaningful network harm, package install failure, or auth/role drift.
- `Medium`: degraded workflow, false-confidence tests, or drift that can mislead operators.
- `Low`: docs-only drift, cosmetic issues, or low-blast-radius maintenance bugs.

## Confidence

- `Confirmed`: reproduced with a command/test or proven by exact static invariant violation.
- `Likely`: strong code-path evidence, but still needs a focused repro before fixing.
- `Needs Repro`: plausible scanner/expert finding without enough proof.
- `False Positive`: retained only when useful to explain why a scanner should not fail.

## Status

- `New`
- `Triaged`
- `Accepted`
- `Fixing`
- `Fixed`
- `Verified`
- `Deferred`
- `Rejected`

## Reproducible Discovery Commands

Baseline gate:

```bash
npm run check:remediation
./bin/lint
dotnet test slskd.sln --no-restore
cd src/web && npm run lint && npm test
bash packaging/scripts/validate-packaging-metadata.sh
bash packaging/scripts/validate-release-copy.sh
```

Focused existing gates:

```bash
scripts/check-route-inventory.sh
scripts/check-controller-csrf.sh
scripts/check-anonymous-endpoints.sh
scripts/check-non-versioned-routes.sh
scripts/check-allowlist-drift.sh
scripts/check-sensitive-placeholders.sh
scripts/check-web-api-paths.sh
scripts/check-web-fetch-csrf.sh
scripts/check-web-mediacore-routes.sh
```

Pattern inventory:

```bash
rg -n "\[FromBody\]\s+string" src/slskd
rg -n "api\.(post|put|patch|delete)\([^\n]*(message|roomName|yaml|contentId|disambiguator)" src/web/src
rg -n "navigate\('/(browse|users|chat)'|state=\{\{\s*user|state:\s*\{\s*user" src/web/src
rg -n "CreateClient\(|new HttpClient|AllowAutoRedirect|GetAsync\(|SendAsync\(" src/slskd -g '*.cs'
rg -n "Path\.Combine|File\.|Directory\.|CopyToAsync|OpenWrite|Create\(" src/slskd -g '*.cs'
rg -n "async void|Task\.Run|WaitAsync\(|SemaphoreSlim|ConcurrentDictionary|IServiceProvider" src/slskd -g '*.cs'
rg -n "ISoulseekClient|SearchScope|BrowseAsync|DownloadAsync|SafetyLimiter" src/slskd -g '*.cs'
```

New bug-council gates:

```bash
scripts/check-web-url-intent.sh
scripts/check-web-json-string-bodies.sh
scripts/check-mutating-role-requirements.sh
scripts/check-outbound-http-guards.sh
scripts/check-path-containment.sh
scripts/check-soulseek-network-health.sh
scripts/check-workflow-trigger-policy.sh
scripts/check-release-asset-matrix.sh
scripts/check-config-option-drift.sh
scripts/check-systemd-permission-matrix.sh
```

## Ranked Ledger

Extended process fields for every row:

- `Duplicate ADR`: known gotcha entry when this row is a regression guard for an existing bug class.
- `Baseline guard`: scanner or test that currently covers the row.
- `Repro status`: command/test/static proof state.
- `Verification command`: command required before `Verified`.
- `False positive reason`: required when confidence becomes `False Positive`.

| ID | Domain | Class | Severity | Confidence | Evidence | Impact | Fix owner area | Regression requirement | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-20260505-001 | Tests/Tooling | Weak or misleading test coverage | Medium | Confirmed | `scripts/check-web-url-intent.sh`; ADR-0001 `0z290` | Route-state-only user navigation can regress silently and break new-tab/direct-load Browse, Users, or Chat workflows. | web | Keep `scripts/check-web-url-intent.sh` in `npm run check:remediation`; add focused route tests for any new user-targeted page. | Accepted |
| BUG-20260505-002 | Tests/Tooling | Primitive JSON body mismatches | Medium | Confirmed | `scripts/check-web-json-string-bodies.sh`; ADR-0001 `0z291` | Web helpers can post invalid JSON to ASP.NET `[FromBody] string` actions, causing room/config/content operations to fail before controller logic runs. | web/backend | Keep `scripts/check-web-json-string-bodies.sh` in `npm run check:remediation`; add helper tests when introducing `[FromBody] string` endpoints. | Accepted |
| BUG-20260505-003 | Backend/Security | API auth/role drift | High | Confirmed | `scripts/check-mutating-role-requirements.sh`; ADR-0001 `0z287` | New mutating endpoints can accidentally inherit read-only authenticated access. | backend | Keep `scripts/check-mutating-role-requirements.sh` in `npm run check:remediation`; add controller authorization tests for new mutating APIs. | Accepted |
| BUG-20260505-004 | Backend/Security | SSRF/redirect/outbound fetch bypass | High | Confirmed | `scripts/check-outbound-http-guards.sh`; ADR-0001 `0z288` | Guarded outbound callers can validate the first URI but follow redirects or use an unguarded client for the actual request. | backend | Keep `scripts/check-outbound-http-guards.sh` in `npm run check:remediation`; add no-redirect/guard tests for any new remote URL feature. | Accepted |
| BUG-20260505-005 | Backend/Security | Filesystem/path containment | High | Confirmed | `scripts/check-path-containment.sh`; ADR-0001 `0z288` | Remote or user-derived filenames can escape intended roots or write unbounded data if path guards are bypassed. | backend | Keep `scripts/check-path-containment.sh` in `npm run check:remediation`; add path traversal and copy-size regressions for new file write paths. | Accepted |
| BUG-20260505-006 | Network Health | Soulseek network-health regressions | High | Confirmed | `scripts/check-soulseek-network-health.sh`; ADR-0001 `0z289` | Saved searches or automations can use protocol scopes or missing cancellation/limiter metadata that behave differently from manual network searches. | backend/web | Keep `scripts/check-soulseek-network-health.sh` in `npm run check:remediation`; add focused tests for any automated Soulseek-facing producer. | Accepted |
| BUG-20260505-007 | Release/Ops | Release/tag/package/version drift | High | Confirmed | `scripts/check-workflow-trigger-policy.sh`; tag-only build policy in `AGENTS.md` | Branch-push release builds or workflow drift can publish unintended artifacts or miss intended tag builds. | packaging | Keep `scripts/check-workflow-trigger-policy.sh` in `npm run check:remediation`; validate workflow trigger changes before release work. | Accepted |
| BUG-20260505-008 | Release/Ops | Release/tag/package/version drift | Medium | Confirmed | `scripts/check-release-asset-matrix.sh`; `packaging/scripts/validate-packaging-metadata.sh` | Workflow asset names can drift away from AUR/RPM/PPA/Homebrew/Winget expectations. | packaging | Keep `scripts/check-release-asset-matrix.sh` in `npm run check:remediation`; update package metadata checks with any asset rename. | Accepted |
| BUG-20260505-009 | Docs/Config | Config/docs/example drift | Medium | Confirmed | `scripts/check-config-option-drift.sh`; `config/slskd.example.yml` | New operator options can ship without an example or documentation anchor. | docs/backend | Keep `scripts/check-config-option-drift.sh` in `npm run check:remediation`; update config docs when adding option classes. | Accepted |
| BUG-20260505-010 | Release/Ops | Installer/systemd/container permission bugs | High | Confirmed | `scripts/check-systemd-permission-matrix.sh`; ADR-0001 `0z286` | Linux packages can install a daemon that cannot write config/data paths as its service user. | packaging | Keep `scripts/check-systemd-permission-matrix.sh` in `npm run check:remediation`; validate tmpfiles/sysusers/service changes together. | Accepted |

## Council Intake

These rows came from read-only expert review and still need adversarial triage before implementation.

| ID | Domain | Class | Severity | Confidence | Evidence | Impact | Fix owner area | Regression requirement | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-20260505-011 | Frontend/Workflow | Frontend URL intent lost at route boundaries | High | Confirmed | `src/web/src/components/Search/Searches.jsx` read `q` from `URLSearchParams`, then applied `decodeURIComponent` again. Fixed by removing the second decode and de-duplicating URL-created searches. | Direct `/searches?q=100%25` could throw `URIError`; literal `%2520` intent could be lost. | web | `cd src/web && npm test -- Searches.test.jsx` covers encoded percent preservation. | Verified |
| BUG-20260505-012 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | Browse, Chat, and Rooms loaded `parsed.tabs || []` and later called `tabs.map`. Fixed by accepting only `Array.isArray(parsed.tabs)`. | Valid JSON with wrong shape, such as `{"tabs":{}}`, could crash core tabbed journeys on load. | web | `cd src/web && npm test -- Browse.test.jsx Chat.test.jsx Rooms.test.jsx` covers corrupted tab storage. | Verified |
| BUG-20260505-013 | Frontend/Workflow | Workflow semantic drift across manual vs automated paths | Medium | Confirmed | `RoomCreateModal` passed `isPrivate`, but `Rooms.createRoom(roomName, isPrivate)` delegated to `joinRoom(roomName)`. Fixed by disabling unsupported private-room creation and throwing if called programmatically. | User could select Private Room, but the UI performed the same public join/create behavior. | web | `cd src/web && npm test -- RoomCreateModal.test.jsx Rooms.test.jsx` verifies unsupported private-room posture. | Verified |
| BUG-20260505-014 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | Room API helpers warned and returned `undefined` on non-array payloads. Fixed by returning `[]` for malformed list responses. | Future callers could crash or render stale state if they did not add their own `|| []`. | web | `cd src/web && npm test -- rooms.test.js` verifies malformed payloads return `[]`. | Verified |
| BUG-20260505-015 | Frontend/Workflow | UI action wiring/state persistence bugs | Low | Confirmed | Chat and Room send actions/context buttons lacked discoverable labels/tooltips. Fixed by adding action labels/titles and room context `Popup` wrappers. | Violated the repo tooltip rule and made icon actions harder to discover/test. | web | `cd src/web && npm run lint` validates the touched JSX; future UI-specific coverage remains useful. | Fixed |
| BUG-20260505-016 | Network Health | Soulseek network-health regressions | High | Confirmed | `MultiSourceDownloadService.FindVerifiedSourcesAsync` called `_client.SearchAsync` directly rather than a safety limiter. Fixed by consuming the Soulseek search limiter before source discovery. | Remediation/swarm verification could bypass search safety budgets and create bursty network searches. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~MultiSourceDownloadServiceSanitizationTests` verifies exhausted limiter prevents Soulseek search. | Verified |
| BUG-20260505-017 | Network Health | Async/concurrency/lifecycle hazards | High | Confirmed | `BackfillFileAsync` waited on a semaphore with cancellation, then unconditionally decremented/released in `finally`. Fixed by tracking whether the semaphore was acquired. | Cancellation while waiting could over-release concurrency and make active backfill accounting negative. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~BackfillSchedulerServiceTests` covers cancellation while waiting. | Verified |
| BUG-20260505-018 | Network Health | Protocol trust/signature fail-open | High | Confirmed | `ParseFlacHeader` returned `string.Empty` on invalid data; caller checked only `hash != null`. Fixed by returning `null` for invalid/non-FLAC headers and requiring a non-blank hash. | Failed probes could be recorded as successful empty hashes and propagated to HashDb/mesh. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~BackfillSchedulerServiceTests` covers invalid FLAC headers. | Verified |
| BUG-20260505-019 | Release/Ops | Release/tag/package/version drift | High | Confirmed | `.github/workflows/release-linux.yml` rewrote AUR `sha256sums` with too few entries after `slskd.tmpfiles` was added. Fixed by including the tmpfiles checksum. | AUR update path could generate invalid PKGBUILDs. | packaging | `bash packaging/scripts/validate-packaging-metadata.sh` verifies the workflow checksum matrix. | Verified |
| BUG-20260505-020 | Release/Ops | Release/tag/package/version drift | High | Confirmed | `.github/workflows/release-linux.yml` ran `bash ../packaging/scripts/validate-aur-pkgbuild-hashes.sh` after `cd packaging/aur`. Fixed to use `../scripts/validate-aur-pkgbuild-hashes.sh`. | AUR update job could fail before pushing metadata. | packaging | `bash packaging/scripts/validate-packaging-metadata.sh` verifies the workflow path. | Verified |
| BUG-20260505-021 | Release/Ops | Installer/systemd/container permission bugs | High | Confirmed | Debian/RPM/COPR packaging copied sysusers but not `slskd.tmpfiles`; RPM spec declared `Source4`. Fixed by copying tmpfiles metadata into package build sources/artifacts. | Package build could fail or omit ownership convergence metadata. | packaging | `bash packaging/scripts/validate-packaging-metadata.sh` verifies tmpfiles copy paths. | Verified |
| BUG-20260505-022 | Backend/Security | Secret/log/error leakage | Medium | Confirmed | `MeshGatewayConfigValidator` logged generated CSRF token/header value. Fixed by logging only generic generation/configuration status. | Logs could replay localhost mesh gateway CSRF header for that session. | backend | `npm run check:remediation` runs `scripts/check-sensitive-placeholders.sh`, which rejects raw CSRF token log templates. | Verified |
| BUG-20260505-023 | Backend/Security | Secret/log/error leakage | Medium | Confirmed | `RelayService` logged relay auth challenge and upload/download tokens. Fixed by logging stable short SHA-256 token IDs instead of raw replayable values. | Debug logs could replay short-lived relay workflows during token TTL. | backend | `npm run check:remediation` runs `scripts/check-sensitive-placeholders.sh`, which rejects raw relay token log templates. | Verified |
| BUG-20260505-024 | Backend/Security | SSRF/redirect/outbound fetch bypass | Medium | Confirmed | Token-bearing notification and Spotify HTTP requests used default redirect-following clients. Fixed by using the shared no-redirect guarded client and extending the outbound guard to notifications. | Redirects could leak notification/access tokens or message bodies to redirected hosts. | backend | `npm run check:remediation` runs `scripts/check-outbound-http-guards.sh`, including notifications/source feeds. | Verified |
| BUG-20260505-025 | Tests/Tooling | Weak or misleading test coverage | High | Confirmed | `scripts/check-non-versioned-routes.sh` skipped a controller if any route was versioned. Fixed so every API route attribute is inspected and non-versioned API routes must be allowlisted. | Mixed legacy+versioned controllers could hide undocumented legacy routes. | tests | `npm run check:remediation` verifies strict non-versioned API route scanning. | Verified |
| BUG-20260505-026 | Tests/Tooling | Weak or misleading test coverage | Medium | Confirmed | `scripts/check-controller-csrf.sh`, anonymous checks, and allowlist drift relied on marker-only regex scans. Fixed by anchoring attribute checks to real attribute lines and API route contexts. | Comments or unrelated attributes could satisfy endpoint-level checks. | tests | `npm run check:remediation` verifies tightened CSRF, anonymous, and allowlist checks. | Verified |
| BUG-20260505-027 | Frontend/Workflow | Frontend URL intent lost at route boundaries | Medium | Confirmed | `Users.jsx` read `location.search` only on mount, so navigating from `/users?user=alice` to `/users?user=bob` while the component stayed mounted left the old user selected. | User profile links could show stale user data when routed within the mounted Users page. | web | `cd src/web && npm test -- Users.test.jsx` verifies URL user changes while mounted. | Verified |
| BUG-20260505-028 | Release/Ops | Installer/systemd/container permission bugs | High | Confirmed | `packaging/docker/slskdn-container-start` executed the server as root when no `PUID`/`PGID` was set. Fixed by chowning the app dir, checking slskdn access, and `exec gosu slskdn "$@"`. | Default Docker containers could run the daemon as root and write root-owned app data. | packaging | `bash packaging/scripts/validate-packaging-metadata.sh` verifies default container startup drops to `slskdn`. | Verified |
| BUG-20260505-029 | Backend/Security | Protocol trust/signature fail-open | Medium | Confirmed | Vendored `RoomListResponseFactory` validated room-name collection counts but accepted negative per-room user counts. Fixed by validating non-negative room user counts before creating `RoomInfo`. | Malformed server room-list payloads could create impossible negative user-count domain values. | backend | `dotnet test vendor/slskNet.Runtime/tests/Soulseek.Tests.Unit/Soulseek.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~ProtocolCountHardeningTests|FullyQualifiedName~RoomListTests"` verifies negative count rejection. | Verified |
| BUG-20260505-030 | Network Health | Soulseek network-health regressions | High | Confirmed | `MultiSourceController` used direct wide `Client.SearchAsync` calls for users, file sources, download-file source lookup, swarm, search, and test endpoints without consuming the shared safety limiter. Fixed by gating each controller search through `TryConsumeSearchBudget`. | Multi-source diagnostic/helper endpoints could bypass Soulseek search budgets and issue large searches even after the limiter was exhausted. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~MultiSourceControllerTests` and `bash scripts/check-soulseek-network-health.sh` verify exhausted budget prevents search and all direct multi-source search paths are guarded. | Verified |
| BUG-20260505-031 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/lib/searches.js` parsed `slskdn_blocked_users` and returned any valid JSON directly, then `blockUser()` and `unblockUser()` called array methods on it. Fixed by returning `[]` unless the parsed value is an array. | A malformed-but-valid blocked-users localStorage value could crash Search response filtering or block/unblock actions. | web | `cd src/web && npm test -- searches.test.js` verifies wrong-shape blocked-user storage is ignored before array operations. | Verified |
| BUG-20260505-032 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/lib/automationRecipes.js` parsed automation state/input localStorage maps without rejecting arrays. Fixed by requiring a non-array object before merging or writing nested recipe state. | Malformed automation state could produce numeric-key junk and break recipe input/state updates. | web | `cd src/web && npm test -- src/components/System/AutomationCenter/index.test.jsx` verifies wrong-shape automation state and inputs are ignored. | Verified |
| BUG-20260505-033 | Frontend/Workflow | UI action wiring/state persistence bugs | Low | Confirmed | `src/web/src/components/System/ExperienceSettings/index.jsx` spread any parsed preference JSON over defaults. Fixed by accepting only non-array objects. | Malformed local preference arrays could leak numeric keys into the form state and create confusing report/save output. | web | `cd src/web && npm test -- src/components/System/ExperienceSettings/index.test.jsx` verifies wrong-shape preferences fall back to defaults. | Verified |
| BUG-20260505-034 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/components/App.jsx` treated any parsed room activity JSON as a timestamp map. Fixed by accepting only non-array objects. | Malformed room-activity storage could skip first-run baselining or compare unread room timestamps against invalid state. | web | `cd src/web && npm test -- App.test.jsx` verifies malformed room activity is ignored and re-baselined. | Verified |
| BUG-20260505-035 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/components/Messaging/Messaging.jsx` parsed workspace storage and read `panelCounter` before validating the top-level shape. Fixed by requiring a non-array object and resetting invalid counters to `0`. | Array-shaped workspace storage could poison the panel counter and create `NaN` panel IDs after opening a new chat/room panel. | web | `cd src/web && npm test -- Messaging.test.jsx` verifies malformed workspace storage creates `chat-1` instead of a poisoned ID. | Verified |
| BUG-20260505-036 | Frontend/Workflow | UI action wiring/state persistence bugs | Low | Confirmed | `src/web/src/lib/audioVerification.js` accepted array-shaped audio verification caches as object maps. Fixed by rejecting arrays. | Malformed cache storage could merge numeric keys into fingerprint cache state and confuse cache-hit behavior. | web | `cd src/web && npm test -- audioVerification.test.js` verifies malformed cache shapes return `{}`. | Verified |
| BUG-20260505-037 | Frontend/Workflow | UI action wiring/state persistence bugs | Low | Confirmed | `src/web/src/components/Player/Visualizer.jsx` returned any parsed active native preset JSON. Fixed by accepting only non-array objects. | Malformed active native preset storage could be treated as preset metadata during native edit/remove operations. | web | `cd src/web && npm test -- Visualizer.test.jsx` covers native preset workflows after the shape guard. | Verified |
| BUG-20260505-038 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/components/System/Events/index.jsx` called `JSON.parse(event.data)` during render. Fixed by formatting valid JSON and falling back to the raw string for malformed data. | One malformed backend event payload could crash the System Events table and block event inspection. | web | `cd src/web && npm test -- src/components/System/Events/index.test.jsx` verifies malformed event data renders without crashing. | Verified |
| BUG-20260505-039 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/lib/communityQualitySignals.js` accepted invalid entries inside an array-shaped localStorage value. Fixed by filtering entries to non-array objects before normalization. | Malformed community quality signal storage could crash local dashboard summaries and reputation signal rendering. | web | `cd src/web && npm test -- communityQualitySignals.test.js` verifies malformed signal entries are ignored. | Verified |
| BUG-20260505-040 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/lib/discoveryInbox.js` accepted invalid entries inside an array-shaped localStorage value. Fixed by filtering entries to non-array objects before `normalizeItem`. | Malformed Discovery Inbox storage could crash candidate review and acquisition handoff flows. | web | `cd src/web && npm test -- discoveryInbox.test.js` verifies malformed discovery items are ignored. | Verified |
| BUG-20260505-041 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/lib/acquisitionPlans.js` accepted invalid entries inside an array-shaped localStorage value. Fixed by filtering entries to non-array objects before `normalizePlan`. | Malformed acquisition plan storage could crash plan execution/review and block bounded search or Wishlist handoffs. | web | `cd src/web && npm test -- acquisitionPlans.test.js` verifies malformed plan entries are ignored. | Verified |
| BUG-20260505-042 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/lib/discoveryShelf.js` accepted invalid entries inside an array-shaped localStorage value. Fixed by filtering entries to non-array objects before summaries and promote handoffs. | Malformed Discovery Shelf storage could crash shelf summaries or promote-preview inbox generation. | web | `cd src/web && npm test -- discoveryShelf.test.js` verifies malformed shelf entries are ignored. | Verified |
| BUG-20260505-043 | Frontend/Workflow | UI action wiring/state persistence bugs | Low | Confirmed | `src/web/src/lib/albumDecisionRules.js` accepted invalid entries inside an array-shaped localStorage value. Fixed by filtering entries to non-array objects before save/replace logic. | Malformed album decision rule storage could crash local rule saves and duplicate replacement. | web | `cd src/web && npm test -- albumDecisionRules.test.js` verifies malformed rule entries are ignored. | Verified |
| BUG-20260505-044 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/lib/listeningHistory.js` accepted invalid entries inside an array-shaped localStorage value. Fixed by filtering entries to non-array objects before stats and seed generation. | Malformed listening history storage could crash listening stats and explicit recommendation seed generation. | web | `cd src/web && npm test -- listeningHistory.test.js` verifies malformed history entries are ignored. | Verified |
| BUG-20260505-045 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/lib/playlistIntake.js` normalized every persisted playlist entry and nested track entry without item-shape checks. Fixed by filtering top-level playlists and nested tracks to non-array objects before normalization. | Malformed playlist intake storage could create phantom playlists or crash review, refresh, and organization workflows. | web | `cd src/web && npm test -- playlistIntake.test.js` verifies malformed playlist and track entries are ignored. | Verified |
| BUG-20260505-046 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/lib/watchlists.js` normalized every persisted watchlist entry and expansion candidate without item-shape checks, and `releaseTypes` assumed an array. Fixed by filtering persisted watchlists/candidates and normalizing non-array release types. | Malformed watchlist storage could create phantom watches or crash similar-artist expansion decisions. | web | `cd src/web && npm test -- watchlists.test.js` verifies malformed watchlist and expansion candidate entries are ignored. | Verified |
| BUG-20260505-047 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `src/web/src/lib/quarantineJury.js` returned `response.data || []` from a list helper. Fixed by returning data only when it is an array. | Malformed quarantine-jury request payloads could escape as object-shaped lists and crash list callers. | web | `cd src/web && npm test -- quarantineJury.test.js` verifies malformed list payloads return `[]`. | Verified |
| BUG-20260505-048 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `src/web/src/lib/listeningParty.js` returned `response.data || []` from a list helper. Fixed by returning data only when it is an array. | Malformed listening-party directory payloads could escape as object-shaped lists and crash directory callers. | web | `cd src/web && npm test -- listeningParty.test.js` verifies malformed list payloads return `[]`. | Verified |
| BUG-20260505-049 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Contacts/Contacts.jsx` stored contact and nearby API payloads with `response.data || []`. Fixed by accepting only arrays before setting list state. | Malformed identity payloads could crash Contacts list rendering or nearby-peer rendering. | web | `cd src/web && npm test -- Contacts.test.jsx` verifies malformed contact and nearby payloads are ignored. | Verified |
| BUG-20260505-050 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Collections/Collections.jsx`, `src/web/src/components/Shares/SharedWithMe.jsx`, and `src/web/src/components/ShareGroups/ShareGroups.jsx` stored collection/share/share-group API payloads with `response.data || []`. Fixed by accepting only arrays before list state or member rendering. | Malformed collection/share payloads could crash collection tables, shared-with-me enrichment, or share-group member display. | web | `cd src/web && npm run lint` plus component-local `asArray` guards cover the touched render paths; dedicated component tests remain a follow-up for Collections/Shares/ShareGroups. | Verified |
| BUG-20260505-051 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Search/SoulseekDiscoveryPanel.jsx` mapped `response.data || []` for similar users and counted the raw payload length. Fixed by accepting only arrays before normalization/counting. | Malformed Soulseek similar-user payloads could crash the discovery panel or show impossible counts. | web | `cd src/web && npm test -- SoulseekDiscoveryPanel.test.jsx` verifies malformed similar-user payloads render as an empty result. | Verified |
| BUG-20260505-052 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Player/PlayerBar.jsx` stored collection, collection item, file, directory, and breadcrumb payloads with `response.data || []` or property fallbacks. Fixed by accepting only arrays before launcher/browser list state. | Malformed player collection or file-browser payloads could crash local playback launch modals. | web | `cd src/web && npm test -- PlayerBar.test.jsx` verifies malformed player launcher/browser list payloads are ignored. | Verified |
| BUG-20260505-053 | Frontend/Workflow | Filesystem/path containment | Medium | Confirmed | `src/web/src/lib/files.js` used `btoa(path)` directly in route segments. Fixed by UTF-8 encoding paths before base64 and URL-encoding the base64 route segment. | File Explorer actions could throw for Unicode filenames/directories or misroute paths whose base64 contained `/` or `+`. | web | `cd src/web && npm test -- files.test.js` verifies Unicode path encoding and encoded file/directory API routes. | Verified |
| BUG-20260505-054 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/components/Search/DiscoveryGraphAtlasPanel.jsx` and `src/web/src/components/Search/DiscoveryGraphModal.jsx` loaded `slskdn.discoveryGraph.savedBranches` arrays without validating entries. Fixed by centralizing saved branch parsing and filtering entries with missing `id`/`title` or invalid shapes. | A malformed saved Discovery Graph branch entry could crash atlas/modal saved-branch rendering. | web | `cd src/web && npm test -- discoveryGraph.test.js` verifies malformed saved branch entries are ignored. | Verified |
| BUG-20260505-055 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | Discovery Graph render paths used `(graph?.edges || []).map/filter`, while only nodes were array-guarded. Fixed by centralizing node/edge list guards and using them in Atlas, AtlasPanel, and Modal. | A malformed graph response with truthy non-array `edges` or `nodes` could crash Discovery Graph atlas/modal rendering. | web | `cd src/web && npm test -- discoveryGraph.test.js` verifies malformed node/edge payloads are treated as empty lists. | Verified |
| BUG-20260505-056 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/lib/events.js` returned raw `response.data` to the System Events table. Fixed by returning an empty list unless the event payload is an array. | A malformed events API payload could make the System Events table call `.map()` on a truthy non-array value. | web | `cd src/web && npm test -- events.test.js src/components/System/Events/index.test.jsx` verifies malformed event lists render as empty tables. | Verified |
| BUG-20260505-057 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `src/web/src/lib/bridge.js` returned `response.data?.clients || []`. Fixed by returning clients only when the payload is an array. | A malformed Bridge clients payload could crash Bridge client rendering. | web | `cd src/web && npm test -- bridge.test.js` verifies malformed client lists return `[]`. | Verified |
| BUG-20260505-058 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Search/AlbumCompletionPanel.jsx` stored `response.data?.albums ?? []` and later called `album.tracks.filter`. Fixed by normalizing album and nested track arrays before rendering. | Malformed MusicBrainz album completion payloads could crash album completion rendering. | web | `cd src/web && npm test -- AlbumCompletionPanel.test.jsx` verifies malformed album and track payloads are ignored. | Verified |
| BUG-20260505-059 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `src/web/src/components/Collections/Collections.jsx` used `response.data?.items || []` for collection item search results. Fixed by accepting only arrays before item result dropdown state. | A malformed collection item search payload could crash the collection item result dropdown. | web | `cd src/web && npm run lint` verifies the touched component; the remaining scan for `response.data?.x || []` is clean. | Verified |
| BUG-20260505-060 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Search/FederatedTasteRecommendationsPanel.jsx` used `response.data?.recommendations || []` and counted the raw payload length. Fixed by accepting only arrays before render state and status counts. | A malformed federated taste recommendation payload could crash recommendation rendering or show impossible counts. | web | `cd src/web && npm test -- FederatedTasteRecommendationsPanel.test.jsx` verifies malformed recommendation payloads render as an empty result. | Verified |
| BUG-20260505-061 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Search/Detail/SearchDetail.jsx` called `response.data.reduce` for user notes without verifying that the response payload was an array. Fixed by mapping only array payloads and ignoring malformed note entries. | A malformed user-notes payload could crash Search Detail before results render. | web | `cd src/web && npm test -- SearchDetail.test.jsx` verifies malformed user note payloads return an empty note map. | Verified |
| BUG-20260505-062 | Frontend/Workflow | UI action wiring/state persistence bugs | Low | Confirmed | `src/web/src/lib/albumDecisionRules.js` called `.map()` on candidate `formatMix`, `warnings`, and `substitutionOptions` via `|| []` fallbacks. Fixed by accepting only arrays and filtering nested option objects. | A malformed album candidate could crash browser-local album decision rule preview/save. | web | `cd src/web && npm test -- albumDecisionRules.test.js` verifies malformed candidate nested fields are ignored. | Verified |
| BUG-20260505-063 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `src/web/src/components/Search/FederatedTasteRecommendationsPanel.jsx` mapped `recommendation.reasons || []` and joined `recommendation.sourceActors` after a length check. Fixed by requiring arrays for both nested fields. | A malformed recommendation entry could crash the Federated Taste list after the top-level recommendation array was valid. | web | `cd src/web && npm test -- FederatedTasteRecommendationsPanel.test.jsx` verifies malformed nested reason/source actor lists are ignored. | Verified |
| BUG-20260505-064 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Search/DiscographyCoveragePanel.jsx` called `.flatMap()`/`.map()` over `coverage.releases` and nested `release.tracks` without shape checks. Fixed by normalizing coverage, releases, and tracks before rendering/counting missing tracks. | A malformed MusicBrainz discography coverage payload could crash the Discography Concierge. | web | `cd src/web && npm test -- DiscographyCoveragePanel.test.jsx` verifies malformed release/track arrays are ignored. | Verified |
| BUG-20260505-065 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `src/web/src/components/System/SourceProviders/index.jsx` normalized provider capabilities and profile provider priority through `|| []`-style assumptions. Fixed by requiring arrays for nested capabilities and priority lists. | A malformed source provider catalog could crash Source Providers cards or policy rendering. | web | `cd src/web && npm test -- src/components/System/SourceProviders/index.test.jsx` verifies malformed nested provider/policy lists normalize to `[]`. | Verified |
| BUG-20260505-066 | Frontend/Workflow | UI action wiring/state persistence bugs | Low | Confirmed | `src/web/src/lib/watchlists.js` summarized `item.expansionCandidates || []` with `.reduce()`. Fixed by reusing `normalizeExpansionCandidates()` before summarizing. | A malformed watchlist expansion candidate field could crash watchlist summary rendering. | web | `cd src/web && npm test -- watchlists.test.js` verifies malformed expansion candidates summarize as empty. | Verified |
| BUG-20260505-067 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Messaging/Messaging.jsx` hydrated conversations, joined rooms, pods, and pod channels with `|| []` list fallbacks. Fixed by accepting only arrays and filtering object-shaped conversation/pod/channel entries before workspace state. | Malformed chat, room, or pod payloads could crash the unified Messaging workspace during hydration. | web | `cd src/web && npm test -- Messaging.test.jsx` verifies malformed server list payloads hydrate without crashing. | Verified |
| BUG-20260505-068 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/App.jsx` used `(conversations || []).length`, `(await rooms.getJoined()) || []`, and `(await rooms.getMessages()) || []` for navigation activity badges. Fixed by requiring arrays before unread chat/room activity counting. | Malformed chat or room activity payloads could crash the app shell polling loop or leave navigation badges in a broken state. | web | `cd src/web && npm test -- App.test.jsx Chat.test.jsx Rooms.test.jsx` verifies malformed navigation activity payloads are ignored. | Verified |
| BUG-20260505-069 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Chat/Chat.jsx` hydrated saved conversations with `(serverConversations || []).filter(...)`. Fixed by requiring an array and filtering object-shaped conversation entries before restoring tabs. | A malformed chat conversation list could crash the legacy Chat workflow or create invalid saved-conversation tabs. | web | `cd src/web && npm test -- App.test.jsx Chat.test.jsx Rooms.test.jsx` verifies malformed conversation payloads do not restore tabs. | Verified |
| BUG-20260505-070 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Rooms/Rooms.jsx` hydrated joined and available rooms with `|| []` list fallbacks. Fixed by requiring arrays before joined-room tab restore, search options, and join/leave refresh state. | Malformed room list payloads could crash the legacy Rooms workflow or create invalid room tabs/options. | web | `cd src/web && npm test -- App.test.jsx Chat.test.jsx Rooms.test.jsx` verifies malformed joined room payloads are ignored. | Verified |
| BUG-20260505-071 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Search/ArtistReleaseRadarPanel.jsx` stored subscription and notification payloads with `response.data || []` and counted `subscription.mutedReleaseGroupIds || []`. Fixed by accepting only arrays, filtering object-shaped entries, and requiring nested muted release groups to be arrays before counting. | Malformed Artist Release Radar payloads could crash radar rendering or show invalid subscription counts. | web | `cd src/web && npm test -- ArtistReleaseRadarPanel.test.jsx` verifies malformed radar lists and nested muted release group lists are ignored. | Verified |
| BUG-20260505-072 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/System/LibraryHealth/index.jsx` stored Library Health grouped issue and issue-list payloads with `.groups || []` / `.issues || []`. Fixed by accepting only arrays of objects before report tables and issue selection state. | Malformed Library Health payloads could crash admin review tables or selection workflows. | web | Focused admin batch: `cd src/web && npm test -- src/components/System/LibraryHealth/index.test.jsx src/components/System/Jobs/index.test.jsx src/components/System/Mesh/index.test.jsx src/lib/realmSubjectIndexes.test.js src/components/System/Mesh/RealmSubjectIndexConflicts.test.jsx src/lib/pods.test.js src/lib/searchActionPreview.test.js src/lib/searchCandidateRanking.test.js src/components/Messaging/Messaging.test.jsx` verifies malformed admin and helper list payloads. | Verified |
| BUG-20260505-073 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/System/Jobs/index.jsx` stored job and swarm job payloads with `response.jobs || []` and trusted `getActiveSwarmJobs()` output. Fixed by accepting only object arrays before analytics and job/swarm rendering. | Malformed job payloads could crash the Jobs dashboard analytics or swarm job cards. | web | Focused admin batch command above verifies malformed job/swarm payloads are ignored. | Verified |
| BUG-20260505-074 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/System/Mesh/index.jsx` trusted rendezvous status objects, discovery users, capability records, and nested record features. Fixed by requiring object status, array users/records, object entries, and array features. | Malformed Soulseek mesh rendezvous payloads could crash the Mesh dashboard or render invalid peer records. | web | Focused admin batch command above verifies malformed rendezvous payloads are ignored. | Verified |
| BUG-20260505-075 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/lib/realmSubjectIndexes.js` and `src/web/src/components/System/Mesh/RealmSubjectIndexConflicts.jsx` used conflict/value list fallbacks without shape checks. Fixed by centralizing conflict/value array guards and using them in summaries, report formatting, and rendering. | Malformed realm conflict payloads could crash summary/report generation or conflict review rendering. | web | Focused admin batch command above verifies malformed conflict/value payloads are ignored. | Verified |
| BUG-20260505-076 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/lib/pods.js`, `src/web/src/components/Pods/Pods.jsx`, and `src/web/src/components/Messaging/Messaging.jsx` trusted pod, member, message, and discovery list payloads with `|| []`. Fixed by requiring arrays and object entries before pod lists, members, messages, discovery results, and Messaging pod channels. | Malformed pod payloads could crash Pods, Messaging pod channels, or discovery result rendering. | web | Focused admin batch command above verifies malformed pod discovery and Messaging pod lists are ignored. | Verified |
| BUG-20260505-077 | Frontend/Workflow | Frontend URL intent lost at route boundaries | Medium | Confirmed | `src/web/src/lib/pods.js` and `src/web/src/components/Pods/Pods.jsx` interpolated pod/channel identifiers directly into API and UI routes. Fixed by URL-encoding pod and channel path segments. | Pod or channel identifiers containing `/`, `?`, `#`, or `%` could route to the wrong API/UI path. | web | Focused admin batch command above verifies pod/channel API path segments are encoded. | Verified |
| BUG-20260505-078 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/PortForwarding/PortForwarding.jsx` and `src/web/src/components/Pods/PortForwarding.jsx` trusted available-port and forwarding-status list payloads. Fixed by accepting only arrays and object-shaped forwarding entries. | Malformed port-forwarding payloads could crash forwarding dashboards during background refresh. | web | Web lint plus focused list-guard implementation covers both mirrored forwarding components; component-specific tests remain a follow-up. | Verified |
| BUG-20260505-079 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/lib/searchActionPreview.js`, `src/web/src/lib/searchCandidateRanking.js`, and `src/web/src/components/Search/Response.jsx` used `sourceProviders` and preferred extension list fields as arrays without shape checks. Fixed by requiring arrays before provider labels, ranking signals, bridge routing checks, and provider badges. | Malformed search responses could crash result rendering, action previews, or ranking. | web | Focused admin batch command above verifies malformed source provider and preferred extension lists are ignored. | Verified |
| BUG-20260505-080 | Frontend/Workflow | Frontend URL intent lost at route boundaries | Medium | Confirmed | `src/web/src/components/Shares/SharedWithMe.jsx` built stream URLs with raw `contentId` path segments. Fixed by URL-encoding the content ID while preserving encoded token query values. | Shared content IDs containing route separators could open the wrong stream endpoint or drop token intent. | web | Web lint and route construction review verify encoded stream content ID path segments; dedicated SharedWithMe test remains a follow-up. | Verified |
| BUG-20260505-081 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `src/web/src/lib/listeningHistory.js`, `src/web/src/lib/playerRadio.js`, `src/web/src/lib/playlistIntake.js`, and `src/web/src/lib/searchResultDeduplication.js` called list operations on persisted/stat/provider fields after `|| []` fallbacks. Fixed by requiring arrays for exported genres, recommendation stat lists, radio plan queries, provider refresh suggestions, and search source providers. | Malformed browser-local workflow data or provider payloads could crash listening stats, smart radio, playlist refresh previews, or search result folding. | web | `cd src/web && npm test -- src/lib/listeningHistory.test.js src/lib/playerRadio.test.js src/lib/searchResultDeduplication.test.js src/lib/playlistIntake.test.js` verifies malformed list fields are ignored. | Verified |
| BUG-20260505-082 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Player/PlayerContext.jsx`, `src/web/src/components/Browse/BrowseSession.jsx`, `src/web/src/components/Browse/Directory.jsx`, and `src/web/src/components/Transfers/TransferGroup.jsx` trusted source-provider/tag and directory file/child lists before rendering or download request construction. Fixed by requiring arrays at play-item, browse-directory, and transfer-list boundaries. | Malformed now-playing metadata or browse/transfer payloads could crash playback badges, Browse sessions, or Transfers cards. | web | `cd src/web && npm test -- src/components/Player/PlayerBar.test.jsx` covers malformed player metadata; `cd src/web && npm run lint` validates the touched Browse/Transfer render paths. | Verified |
| BUG-20260505-083 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/components/Chat/ChatSession.jsx` accepted any conversation object and rendered `conversation.messages || []`; `src/web/src/components/Shared/UserCard.jsx` accepted any interest payload and rendered liked/hated fields with `|| []`. Fixed by normalizing conversation objects, message arrays, interest objects, and liked/hated arrays before state/render use. | Malformed chat or native-interest payloads could crash the active chat panel or user card popups. | web | `cd src/web && npm test -- src/components/Chat/ChatSession.test.jsx` verifies malformed chat message lists are ignored; Web lint validates the UserCard path. | Verified |
| BUG-20260505-084 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | Search graph handoffs in `SearchDetail`, `SearchListRow`, `MusicBrainzLookup`, `Response`, and `SongIDPanel` used `graph.nodes || []` or `run.tracks || []` before queueing nearby searches, and SongID provenance rendered `signals || []`. Fixed by requiring arrays before graph/query/provenance list operations. | Malformed graph or SongID payloads could crash search discovery actions or queue invalid nearby searches. | web | `cd src/web && npm test -- src/components/Search/List/SearchListRow.test.jsx src/components/Search/Detail/SearchDetail.test.jsx` plus Web lint verifies touched search paths. | Verified |
| BUG-20260505-085 | Frontend/Workflow | UI action wiring/state persistence bugs | Low | Confirmed | `SearchFilterModal`, System Bridge, Federation diagnostics, and Security adversarial settings joined/mapped filter, client, warning, padding, and Obfs4 bridge-line fields through `|| []` fallbacks. Fixed by requiring arrays before form value joins and diagnostic/settings rendering. | Malformed filter state or admin diagnostics/settings payloads could crash modals and system settings panels. | web | `cd src/web && npm run lint` verifies the touched admin/filter render paths; focused component tests remain a follow-up if these panels get behavior changes. | Verified |
| BUG-20260505-086 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | Browse, Chat, and Rooms restored persisted tab arrays without item-shape validation and accepted invalid counters. Fixed by filtering persisted tabs to object-shaped entries, normalizing labels/usernames/room names, and resetting invalid counters. | Malformed localStorage could crash tab hydration or create poisoned tab IDs in core workflows. | web | `cd src/web && npm test -- Browse.test.jsx Chat.test.jsx Rooms.test.jsx` verifies malformed persisted tabs/counters are ignored. | Verified |
| BUG-20260505-087 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `BrowseSession` trusted cached browse state and server directory lists before reading directory/file fields. Fixed by requiring object-shaped cached state and normalizing directory/file lists before tree/count rendering. | Malformed cached or API browse data could crash user browsing or restore invalid selected directories. | web | `cd src/web && npm test -- Browse.test.jsx` and focused BrowseSession normalization coverage in the broader batch verify malformed browse state is ignored. | Verified |
| BUG-20260505-088 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | Swarm analytics helpers and the analytics view accepted truthy non-array peer ranking and recommendation payloads. Fixed by returning/storing arrays only. | Malformed swarm analytics payloads could crash System analytics cards. | web | `cd src/web && npm test -- src/components/System/SwarmAnalytics/index.test.jsx` verifies malformed list payloads render as empty. | Verified |
| BUG-20260505-089 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | Swarm visualization rendered `traceSummary.peers` and `bytesBySource` without verifying array/map shapes. Fixed by requiring arrays for peers and non-array objects for source-byte maps. | Malformed swarm trace payloads could crash visualization and diagnostics review. | web | `cd src/web && npm test -- src/components/System/SwarmVisualization/index.test.jsx` verifies malformed trace lists/maps are ignored. | Verified |
| BUG-20260505-090 | Frontend/Workflow | Frontend URL intent lost at route boundaries | Low | Confirmed | Invalid `/system/:tab` URLs left System on an invalid tab index. Fixed by redirecting missing or unknown tab segments to `/system/info`. | Stale copied URLs could land on a blank or incoherent System view. | web | `cd src/web && npm test -- src/components/System/System.test.jsx` verifies unknown System tabs redirect to Info. | Verified |
| BUG-20260505-091 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | Transfers normalized top-level transfer users but still trusted nested directories and files before filtering hidden files or refreshing queue positions. Fixed by recursively normalizing transfer users, directories, and files. | Malformed transfer queue payloads could crash Transfers or bulk queue refreshes. | web | `cd src/web && npm test -- Transfers.test.jsx` verifies malformed nested transfer directories/files are ignored. | Verified |
| BUG-20260505-092 | Network Health | Soulseek network-health regressions | High | Confirmed | The compatibility user browse endpoint called `ISoulseekClient.BrowseAsync()` without consuming the shared browse limiter or passing request cancellation. Fixed by injecting `ISoulseekSafetyLimiter`, rejecting exhausted browse budgets with 429, and propagating cancellation. | Compatibility API callers could bypass Soulseek browse budgets and continue peer work after the HTTP request was canceled. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~UsersCompatibilityControllerTests` verifies limiter rejection and cancellation propagation. | Verified |
| BUG-20260505-093 | Backend/Security | SSRF/redirect/outbound fetch bypass | High | Confirmed | AcoustID, MusicBrainz, release-graph, and Pushbullet integrations used default redirect-following HTTP clients. Fixed by using `OutboundUriGuard.NoRedirectHttpClientName` for those outbound calls. | Redirects could leak request bodies, access tokens, or lookup traffic to a redirected host. | backend | Focused integration unit slice: `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~MusicBrainz|FullyQualifiedName~AcoustId|FullyQualifiedName~Pushbullet"` plus direct `CreateClient()` rescan verify the touched integrations. | Verified |
| BUG-20260505-094 | Backend/Security | Secret/log/error leakage | Medium | Confirmed | AcoustID warning/debug logs included the full audio fingerprint. Fixed by logging a short SHA-256-derived fingerprint ID instead of the raw fingerprint. | Logs could expose stable audio fingerprints that should not be copied into operational telemetry. | backend | Direct scan for `fingerprint {Fingerprint}` is clean; the focused AcoustID test slice compiles the redaction helper. | Verified |
| BUG-20260505-095 | Release/Ops | Workflow semantic drift across manual vs automated paths | High | Confirmed | Docs and repo instructions said build tags can be `build-main-*` or `build-dev-*`, but `build-on-tag.yml` triggered and parsed only `build-main-*`. Fixed the trigger/parser/error text and the workflow trigger scanner. | Dev build tags could be pushed without running the expected release build workflow. | packaging | `bash scripts/check-workflow-trigger-policy.sh` verifies both build tag families and tag-only release policy. | Verified |
| BUG-20260505-096 | Release/Ops | Release/tag/package/version drift | Medium | Confirmed | `ci.yml` and `ci-enhancements.yml` used regex-looking tag globs with `+`, which GitHub treats as literal glob text. Fixed the tag globs and added a scanner rejection for regex quantifiers in tag filters. | Version tags could fail to trigger CI paths intended for tag-addressable builds. | packaging | `bash scripts/check-workflow-trigger-policy.sh` verifies glob-safe tag filters. | Verified |
| BUG-20260505-097 | Release/Ops | Config/docs/example drift | High | Confirmed | Helm/TrueNAS charts defaulted to the unpublished/stale `slskd/slskdn` image and ignored env vars such as `SLSKD_USERNAME`, `SLSKD_PASSWORD`, `SLSKD_LISTEN_PORT`, and `SLSKD_SOULSEEK_LISTEN_PORT`. Fixed chart values/questions/README to use `ghcr.io/snapetech/slskdn` and real runtime env names, and removed the unsupported no-listen key. | Kubernetes/TrueNAS installs could start the wrong image or ignore user-supplied credentials and ports. | packaging | `bash packaging/scripts/validate-packaging-metadata.sh` asserts current image/env names and rejects legacy ignored names. | Verified |
| BUG-20260505-098 | Network Health | Soulseek network-health regressions | High | Confirmed | `SourceDiscoveryService` ran continuous `ISoulseekClient.SearchAsync()` cycles without consuming the shared Soulseek search budget. Fixed by injecting `ISoulseekSafetyLimiter`, charging `source-discovery`, and skipping cycles when the budget is exhausted. | Background source discovery could bypass user-configured search budgets and continue network searches after other producers were rate-limited. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~SourceDiscoveryServiceTests` and `bash scripts/check-soulseek-network-health.sh` verify exhausted discovery budget prevents a search. | Verified |
| BUG-20260505-099 | Network Health | Soulseek network-health regressions | Medium | Confirmed | Native `UsersController.Browse` consumed the browse limiter but did not pass request cancellation to `BrowseAsync`. Fixed by accepting an action cancellation token, passing it to Soulseek, and rethrowing request cancellations. | Canceled browse HTTP requests could leave peer browse work running after the caller disconnected. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~UsersControllerTests` verifies limiter rejection and cancellation propagation. | Verified |
| BUG-20260505-100 | Network Health | Soulseek network-health regressions | Medium | Confirmed | Backfill candidate lookup accepted unbounded `limit` values and called `GetUserStatusAsync()` without cancellation. Fixed by clamping candidate limits to `1..100`, checking cancellation during iteration, and passing the caller token to status lookups. | Large candidate requests could fan out into excessive live status checks and continue after request cancellation. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~BackfillSchedulerServiceTests` verifies limit clamping and status cancellation. | Verified |
| BUG-20260505-101 | Network Health | Soulseek network-health regressions | Medium | Confirmed | Rescue-mode multi-source downloads were started with `CancellationToken.None`. Fixed by passing the activation token into the background task and download call. | Canceled rescue activation could still launch multi-source peer work. | backend | Focused unit slice compiles the rescue path; direct scan verifies the rescue download no longer passes `CancellationToken.None`. | Verified |
| BUG-20260505-102 | Network Health | Unbounded input/output/copy loops | High | Confirmed | `ContentVerificationService` created a verification task for every candidate source and awaited `Task.WhenAll`, so one request could probe all candidates concurrently. Fixed by adding a per-request `MaxConcurrentVerificationProbes` semaphore. | Verification could create visible canceled-transfer noise against many peers at once and overload local/network resources. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~ContentVerificationServiceTests` verifies concurrent probes stay within the cap. | Verified |
| BUG-20260505-103 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | MediaCore dropdowns mapped `supportedAlgorithms.algorithms` and `availableStrategies.strategies` through optional chaining plus `|| []`, and Integrations accepted `vpn.portForwards || []`. Fixed by requiring arrays before dropdown/list rendering. | Malformed MediaCore or VPN integration payloads could crash System panels. | web | `cd src/web && npm test -- src/components/System/MediaCore/index.test.jsx src/components/System/Integrations/index.test.jsx` and focused lint verify malformed list guards compile/render. | Verified |
| BUG-20260505-104 | Release/Ops | Config/docs/example drift | Medium | Confirmed | Homebrew and Flatpak docs still used ignored `SLSKD_LISTEN_PORT` / `SLSKD_SOULSEEK_LISTEN_PORT`, and build docs still described only `build-main-*` tag builds. Fixed docs and added validation checks for the current env/tag names. | Operators following package docs could set ignored ports or miss the supported dev tag build path. | docs/packaging | `bash packaging/scripts/validate-packaging-metadata.sh` and `bash scripts/check-workflow-trigger-policy.sh` verify the docs no longer regress to stale env/tag names. | Verified |
| BUG-20260505-105 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | Messaging available-room search, RoomJoinModal, System Files Explorer, System Network capabilities, Soulseek Discovery, and Pods still used `|| []` or optional-chain list rendering for server/persisted lists. Fixed by requiring arrays before state updates, render maps, and tag/message lists. | Malformed payloads could crash room search/join, file explorer, network dashboard, Soulseek discovery, or pod discovery/detail rendering. | web | `cd src/web && npm test -- Messaging.test.jsx Rooms.test.jsx SoulseekDiscoveryPanel.test.jsx`; focused Web lint covers Files/Network/Pods render paths where no dedicated tests exist. | Verified |
| BUG-20260505-106 | Backend/Security | SSRF/redirect/outbound fetch bypass | Medium | Confirmed | NAT public-IP detection and the GitHub release helper constructed direct `HttpClient` instances with default redirect following. Fixed both direct clients to use handlers with `AllowAutoRedirect = false`. | Redirecting public-IP/GitHub helper requests could silently follow to a different host and weaken outbound fetch posture. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~NatDetection|FullyQualifiedName~GitHub"` and `bash scripts/check-outbound-http-guards.sh` verify the clients compile and guarded-client scanner stays green. | Verified |
| BUG-20260505-107 | Frontend/Workflow | Primitive JSON body mismatches | Medium | Confirmed | `src/web/src/lib/mediacore.js` sent pod backfill last-seen timestamps as a bare number to `PodMessageBackfillController.UpdateLastSeenTimestamp([FromBody] long timestamp)`. Fixed by serializing the JSON primitive and asserting the helper request body. | Last-seen updates could depend on Axios primitive serialization/content-type behavior and fail to bind on the backend. | web | `cd src/web && npm test -- src/lib/mediacore.test.js` verifies `updateLastSeenTimestamp()` sends the literal JSON number body. | Verified |
| BUG-20260505-108 | Frontend/Workflow | Frontend URL intent lost at route boundaries | Medium | Confirmed | MediaCore pod workflow helpers interpolated raw `podId`, `channelId`, `contentId`, and variant route segments. Fixed storage, backfill, opinion, and channel helpers to URL-encode each dynamic path segment. | Slash-bearing pod/channel/content identifiers could cross route boundaries and call the wrong workflow endpoint. | web | `cd src/web && npm test -- src/lib/mediacore.test.js` verifies slash-bearing pod/channel/content identifiers are encoded per path segment; direct `rg` rescan confirms no raw `${podId}`/`${channelId}` segments remain in `mediacore.js`. | Verified |
| BUG-20260505-109 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `MediaCore` descriptor verification wrote results into the pod-message signature result state while the descriptor panel rendered `descriptorVerificationResult`; adjacent Shares/Search/Network/MediaCore views still rendered nested list fields directly. Fixed descriptor state wiring and guarded share contents, search warnings/reasons, mesh warnings/workers, and descriptor warnings. | Descriptor verification appeared to do nothing, and malformed nested payloads could crash admin/search/share panels. | web | `cd src/web && npm test -- src/lib/mediacore.test.js src/components/System/Network/index.test.jsx src/components/System/MediaCore/index.test.jsx` plus focused Web lint verify the workflow and list guard batch. | Verified |
| BUG-20260505-110 | Frontend/Workflow | Frontend URL intent lost at route boundaries | Medium | Confirmed | Additional Web helpers in `slskdn.js`, `libraryHealth.js`, `collections.js`, `identity.js`, `wishlist.js`, and `bridge.js` interpolated usernames, job IDs, scan/issue IDs, collection/share IDs, contact IDs, wishlist IDs, and transfer IDs directly into route segments. Fixed each dynamic segment with `encodeURIComponent` and added focused helper tests. | Identifiers containing `/`, `?`, or `%` could route to the wrong resource or drop intent at API boundaries. | web | `cd src/web && npm test -- src/lib/slskdn.test.js src/lib/libraryHealth.test.js src/lib/collections.test.js src/lib/identity.test.js src/lib/wishlist.test.js src/lib/bridge.test.js` verifies slash-bearing IDs are encoded; direct route-segment rescan is clean for the touched identifier names. | Verified |
| BUG-20260505-111 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | MediaCore result panels still used malformed-array `.length` checks and direct maps for domain content IDs, traversal nodes, graph nodes, inbound links, similar matches, import errors, aggregate variants, member affinities, and supported algorithms. Fixed those render paths to require arrays or object maps before counting/rendering. | Malformed MediaCore result payloads could crash or misreport admin workflow panels. | web | `cd src/web && npm test -- src/components/System/MediaCore/index.test.jsx` plus focused Web lint and direct optional-length rescan verify the guards compile and the previous pattern is clean. | Verified |
| BUG-20260505-112 | Backend/Security | Filesystem/path containment | High | Confirmed | GitHub CodeQL alert `cs/user-controlled-bypass` #2552 flagged `RelayController.DownloadFile` because a caller-provided relay filename header influenced the file-serving path. Fixed by caching relay download tokens with a server-side filename record and serving only the filename returned by credential validation. | A relay download request could keep file selection coupled to request-controlled header state, weakening static proof around path containment and file serving. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~RelayControllerModerationTests|FullyQualifiedName~RelayServiceTests"` verifies trusted filename selection and header tamper/invalid-header regressions. | Verified |
| BUG-20260505-113 | Backend/Security | Config validator defined but not registered | High | Confirmed | `MeshGatewayOptions.Validate()` required API keys for non-localhost gateway binding, but `Program.cs` only bound options and `MeshGatewayAuthMiddleware` allowed non-localhost requests when no API key was configured. Fixed startup option validation and runtime fail-closed middleware behavior. | Mesh gateway could be enabled in an invalid remote-access posture without startup failure or middleware rejection. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~MeshGatewayAuthMiddlewareTests` verifies non-localhost access without a configured API key is rejected. | Verified |
| BUG-20260505-114 | Frontend/Workflow | Frontend URL intent lost at route boundaries | Medium | Confirmed | `/pods/:podId` and `/pods/:podId/channels/:channelId` redirected to generic `/messages`, and `Pods.jsx` still interpolated raw pod/channel IDs for channel URLs. Fixed parameterized pod routes to open the pod Messaging workspace and encoded pod/channel URL segments. | Deep-linked pod/channel intent could be discarded, and slash-bearing IDs could cross route boundaries. | web | `cd src/web && npm test -- Messaging.test.jsx` plus direct route-segment rescan verifies pod routes preserve intent and encoded URLs. | Verified |
| BUG-20260505-115 | Frontend/Workflow | Frontend URL intent lost at route boundaries | Medium | Confirmed | Bridged Search result download/stream action URLs interpolated raw `searchId` and `itemId`, and Search creation navigation interpolated raw created IDs. Fixed each segment with `encodeURIComponent`/`segment()` and added a route-builder regression. | Slash/question/percent-bearing search identifiers could call or navigate to the wrong action route. | web | `cd src/web && npm test -- Response.test.jsx Searches.test.jsx SearchListRow.test.jsx` verifies encoded search item action paths. | Verified |
| BUG-20260505-116 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `QuarantineJury` mapped `review.routeAttempts` and `review.verdicts` without requiring arrays; MediaCore stored `getChannels()` directly and joined `rec.supportingFactors` without requiring an array. Fixed these payload boundaries with array guards. | Malformed review/channel/recommendation payloads could crash system review panels. | web | `cd src/web && npm test -- src/components/System/QuarantineJury/index.test.jsx src/components/System/MediaCore/index.test.jsx` verifies malformed lists are treated as empty. | Verified |
| BUG-20260505-117 | Release/Ops | Installer/systemd/container permission bugs | High | Confirmed | `release-linux.yml` copied AUR files without `slskd.tmpfiles`, and `release-ppa.yml` omitted `usr/lib/tmpfiles.d/slskd.conf` while package metadata expected tmpfiles rules. Fixed both workflows and added packaging validation assertions. | AUR/PPA package publication could reference missing source files or miss daemon ownership convergence rules. | packaging | `bash packaging/scripts/validate-packaging-metadata.sh` verifies release workflows copy tmpfiles into AUR and PPA source structures. | Verified |
| BUG-20260505-118 | Release/Ops | Release/tag/package/version drift | Medium | Confirmed | `packaging/snap/snapcraft.yaml` pinned `0.24.5-slskdn.170` while current stable metadata is `2026050500-slskdn.224`. Fixed Snap version/source/checksum and added validation against stable package metadata. | Snap builds could publish an old binary while other package channels point at the current release. | packaging | `bash packaging/scripts/validate-packaging-metadata.sh` verifies Snap version, source release, and checksum match stable metadata. | Verified |
| BUG-20260505-119 | Release/Ops | Release copy / asset-name drift | Low | Confirmed | `.github/release-notes/main.md.tmpl` listed `slskdn-main-linux-x64.zip` and `slskdn-main-linux-arm64.zip` while release assets use `slskdn-main-linux-glibc-*`. Fixed the template and extended release asset matrix validation. | Generated release notes could direct users to stale asset names. | packaging | `bash scripts/check-release-asset-matrix.sh` verifies release note asset names match the current matrix. | Verified |
| BUG-20260505-120 | Backend/Security | CSRF and cookie-auth mutation gaps | High | Confirmed | `ApplicationController.DumpMemory` created and returned a full memory dump via `GET /api/v0/application/dump`. Fixed the endpoint to require POST and added a GET rejection regression. | Cookie-authenticated admin browsers could be induced to start an expensive, secret-bearing dump over a safe method. | backend | `dotnet test tests/slskd.Tests/slskd.Tests.csproj --no-restore --filter FullyQualifiedName~DumpTests` verifies POST behavior and GET 405. | Verified |
| BUG-20260505-121 | Backend/Security | Secret/log/error leakage | High | Confirmed | `Application.cs` logged option diffs and full Soulseek options via raw `ToJson()`. Fixed per-property secret redaction and full Soulseek option logging through `Options.Redact()`. | Config reload/debug logs could expose passwords, API keys, tokens, and proxy credentials. | backend | Direct scan for raw option-diff `ToJson()` logging is clean; `./bin/lint` compiles the redaction path. | Verified |
| BUG-20260505-122 | Backend/Security | SSRF/redirect/outbound fetch bypass | Medium | Confirmed | `HttpTunnelTransport` and `MeekTransport` created default redirect-following `HttpClient` instances while sending tunnel/domain-fronting headers. Fixed both to use `OutboundUriGuard.CreateNoRedirectHandler()` and broadened the outbound HTTP scanner to repo-wide app code with an allowlist. | Redirects could move tunnel/meek requests and custom headers to unintended hosts. | backend | `bash scripts/check-outbound-http-guards.sh` verifies guarded outbound HTTP callers use no-redirect clients across `src/slskd`. | Verified |
| BUG-20260505-123 | Tests/Tooling | Weak or misleading test coverage | High | Confirmed | `check-path-containment.sh` did not encode the relay invariant from CodeQL alert #2552, and `check-outbound-http-guards.sh` only scanned selected directories. Fixed relay-specific path-containment assertions and broadened outbound HTTP guard scanning. | Previously fixed security classes could recur outside literal marker checks while remediation passed. | tests/backend | `bash scripts/check-outbound-http-guards.sh && bash scripts/check-path-containment.sh` verifies the strengthened scanners. | Verified |
| BUG-20260506-001 | Release/Ops | Workflow YAML syntax drift | High | Confirmed | GitHub Actions showed repeated 0-second failures for `.github/workflows/release-linux.yml` and `.github/workflows/release-ppa.yml` on every push, with no jobs/logs. Local YAML parsing found tab indentation in those release workflows and an unindented multiline body in disabled `check-upstream-access.yml`. | GitHub rejected workflow files before job planning, creating noisy CI failures and hiding whether real release jobs would run when manually dispatched or release-triggered. | packaging | `bash scripts/check-workflow-yaml-syntax.sh` and `npm run check:remediation` verify workflow YAML parses and contains no tab characters. | Verified |
| BUG-20260506-002 | Backend/Security | Local control HTTP client over-hardening | High | Confirmed | Live deploy to `kspls0` showed Gluetun status polling failing with `host '127.0.0.1' resolves to non-public address 127.0.0.1` after the public outbound SSRF guard was reused for the configured local Gluetun control endpoint. | The daemon stayed in VPN-waiting state and did not complete normal Soulseek startup on hosts using loopback Gluetun integration. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~OutboundUriGuardTests|FullyQualifiedName~GluetunTests|FullyQualifiedName~VPNServiceTests"` and `bash scripts/check-outbound-http-guards.sh` verify Gluetun uses a local no-redirect client while public outbound callers remain guarded. | Verified |
| BUG-20260506-003 | Network Health | Protocol scalar hardening overreach | High | Confirmed | Live `kspls0` logs after reconnect showed repeated distributed search drops: `The distributed search token must be equal to or greater than zero` with negative token values from real peers. | Valid distributed search requests were rejected after login, reducing participation in the Soulseek distributed network and spamming warnings. | backend | `dotnet test vendor/slskNet.Runtime/tests/Soulseek.Tests.Unit/Soulseek.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~DistributedSearchRequestTests|FullyQualifiedName~ProtocolScalar"` verifies distributed search tokens accept the full signed `int` range while real count/id guards remain. | Verified |
| BUG-20260506-004 | Release/Ops | Release/tag/package/version drift | Medium | Confirmed | Council release scan found `packaging/snap/snapcraft.yaml` still on `2026050500-slskdn.224` while stable package metadata was `2026050600-slskdn.227`; `packaging/scripts/update-stable-release-metadata.sh` also skipped Snap. | Snap could publish or advertise an older stable binary after the rest of the package matrix advanced. | packaging | `bash packaging/scripts/validate-packaging-metadata.sh` verifies Snap version/source/checksum match stable metadata, and the updater now rewrites Snap. | Verified |
| BUG-20260506-005 | Release/Ops | Release/tag/package/version drift | High | Confirmed | `packaging/flatpak/io.github.slskd.slskdn.yml` embedded the release archive and desktop/metainfo/icon entries inside an unclosed wrapper config heredoc instead of the `sources:` section; YAML parsing alone did not catch the structural drift. | Flatpak packaging could build from a malformed manifest or silently omit expected sources/assets. | packaging | `scripts/check-flatpak-manifest-structure.sh` verifies the wrapper/config heredocs close and the archive/assets sit under the slskdn module sources block. | Verified |
| BUG-20260506-006 | Release/Ops | Workflow semantic drift across manual vs automated paths | High | Confirmed | `.github/workflows/codeql.yml` installed .NET 8 while `global.json`, CI, and `src/slskd/slskd.csproj` target .NET 10. | CodeQL C# analysis could fail or analyze a divergent build graph instead of current backend code. | packaging | `scripts/check-codeql-dotnet-version.sh` compares CodeQL setup with `global.json`/target framework and rejects divergent hard-coded setup-dotnet versions. | Verified |
| BUG-20260506-007 | Release/Ops | Workflow semantic drift across manual vs automated paths | High | Confirmed | `.github/workflows/upstream-release.yml` had a six-hour schedule with write permission and created/pushed release tags, which triggers release builds without explicit human tag creation. | Scheduled automation could start a release build or package publish path contrary to the tag-only build policy. | packaging | `scripts/check-workflow-trigger-policy.sh` now rejects scheduled workflows that create or push release tags; the upstream sync workflow is manual-only. | Verified |
| BUG-20260506-008 | Frontend/Workflow | Frontend URL intent lost at route boundaries | Medium | Confirmed | `src/web/src/components/Wishlist/Wishlist.jsx` linked last search results with raw `item.lastSearchId`. | Search ids containing `/`, `?`, or `%` could cross route boundaries or open the wrong search result URL. | web | `cd src/web && npm test -- Wishlist.test.jsx` verifies slash/question/percent ids are encoded; `scripts/check-web-route-segment-encoding.sh` rejects raw Wishlist search links. | Verified |
| BUG-20260506-009 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/lib/wishlist.js` and `Wishlist.jsx` stored raw `/wishlist` payloads before `items.map` and request summary reductions. | A malformed Wishlist list response could crash the Wishlist page. | web | `cd src/web && npm test -- Wishlist.test.jsx` verifies malformed list payloads render as an empty Wishlist; `scripts/check-web-list-shape-guards.sh` rejects raw `setItems(data)`. | Verified |
| BUG-20260506-010 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `src/web/src/lib/shares.js` and `src/web/src/components/System/Shares/index.jsx` trusted `/shares` as a host map and each host value as an array before `Object.entries` and `.map()`. | Malformed shares payloads could crash System Shares during refresh or scan state transitions. | web | `cd src/web && npm test -- src/components/System/Shares/index.test.jsx` verifies malformed host maps/per-host values are ignored; `scripts/check-web-list-shape-guards.sh` guards the pattern. | Verified |
| BUG-20260506-011 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `src/web/src/components/Shares/SharedWithMe.jsx` checked `manifest.items.length` and mapped `manifest.items` without requiring an array. | Malformed shared-collection manifests could crash the incoming share modal. | web | `cd src/web && npm test -- src/components/Shares/SharedWithMe.test.jsx` verifies malformed manifest items render the empty-collection state. | Verified |
| BUG-20260506-012 | Backend/Security | API auth/role drift | High | Confirmed | `SecurityController` exposed security dashboard, event, ban, network, circuit, transport, and peer telemetry to any authenticated role through controller-level `AuthPolicy.Any`. | Read-only or guest accounts could inspect sensitive security telemetry, peer details, and anonymity transport state. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~SecurityControllerTests"` verifies controller-level administrator role authorization. | Verified |
| BUG-20260506-013 | Backend/Security | Anonymous/public surface expansion | Medium | Confirmed | Anonymous `GET /api/v0/application/build` could force a GitHub update check with `checkForUpdates=true` when no cached check existed. | Anonymous traffic could trigger outbound work and make a public metadata endpoint stateful. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~ApplicationControllerTests"` verifies the public build endpoint does not call `CheckVersionAsync`, even when `checkForUpdates=true`. | Verified |
| BUG-20260506-014 | Docs/Config | Weak or misleading test coverage | Medium | Confirmed | `scripts/check-route-inventory.sh` failed after recent controller auth/signature changes because `docs/system-surfaces-current.md` was stale. | Route/auth surface drift could hide in generated docs until the next unrelated scanner run. | docs/tests | `scripts/generate-route-inventory.sh docs/system-surfaces-current.md && scripts/check-route-inventory.sh` verifies regenerated route inventory stays current. | Verified |
| BUG-20260506-015 | Release/Ops | Release/tag/package/version drift | High | Confirmed | `.NET 10` app target was paired with active Flatpak/Proxmox/docs runtime metadata that previously referenced `.NET 8`; the runtime matrix scanner now verifies package/docs targets. | Install/package docs could provision an incompatible runtime for current backend binaries. | packaging | `scripts/check-dotnet-runtime-matrix.sh`, `scripts/check-release-asset-matrix.sh`, and packaging metadata/release-copy validation verify the active runtime/package matrix. | Verified |
| BUG-20260506-016 | Backend/Security | SSRF/redirect/outbound fetch bypass | High | Confirmed | `Program.cs` and `SocialFederation/ServiceCollectionExtensions.cs` registered ActivityPub key-fetch/federation HTTP clients with plain no-redirect handlers after preflight URI checks. | DNS rebinding between validation and connect could bypass the public outbound address invariant. | backend | `scripts/check-outbound-http-guards.sh` now requires ActivityPub and share backfill clients to use `OutboundUriGuard.CreateNoRedirectHandler()`. | Verified |
| BUG-20260506-017 | Backend/Security | Protocol trust/signature fail-open | High | Confirmed | `ShareGrantAnnouncementService` ingested `SHAREGRANT:` private-message payloads without binding `OwnerUserId` to the Soulseek PM sender. | A peer could persist a grant claiming another owner and attach an attacker-controlled endpoint/token for later backfill. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~ShareGrantAnnouncementServiceTests` verifies sender/owner mismatch rejection and matching sender ingestion. | Verified |
| BUG-20260506-018 | Backend/Security | Filesystem/path containment | High | Confirmed | Recursive file listings skipped only `FileAttributes.System`, not `FileAttributes.ReparsePoint`, after validating only the starting root. | A symlink/reparse point inside an allowed root could lead recursive listing outside the intended tree. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~FilesControllerSecurityTests` and `scripts/check-path-containment.sh` verify recursive listings skip reparse points. | Verified |
| BUG-20260506-019 | Frontend/Workflow | Frontend URL intent lost at route boundaries | Medium | Confirmed | `DiscoveryGraphAtlasPanel` hydrated from `location.search` only while mounted but its effect depended only on `persistRoute`. | In-app navigation from one `/discovery-graph?...` query to another could leave the previous graph visible. | web | `cd src/web && npm test -- DiscoveryGraphAtlasPanel.test.jsx` verifies the mounted route rebuilds when the query string changes. | Verified |
| BUG-20260506-020 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `Searches.jsx` assumed SignalR `list` events were arrays before calling `reduce`. | A malformed hub list event could crash the Searches page and drop the search workflow. | web | `cd src/web && npm test -- Searches.test.jsx` verifies malformed `list` events are treated as an empty search list; `scripts/check-web-list-shape-guards.sh` rejects the raw reduce pattern. | Verified |
| BUG-20260506-021 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `Messaging.jsx` validated the persisted workspace wrapper and panels array but returned panel entries verbatim. | Corrupted localStorage panel objects could render invalid panels or poison future panel IDs/state. | web | `cd src/web && npm test -- Messaging.test.jsx` verifies malformed panel entries are filtered and valid panels still restore. | Verified |
| BUG-20260506-022 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | Discovery Graph reports, SongID lane metrics, MediaCore maps, and native MilkDrop preset lists still used permissive `|| {}` / `|| []` fallbacks before object/list operations. | Malformed object/list payloads could crash search/media panels or native visualizer preset handling. | web | `cd src/web && npm test -- src/lib/discoveryGraph.test.js src/components/Player/visualizers/nativeMilkdropEngine.test.js` plus `scripts/check-web-list-shape-guards.sh` verify the guarded patterns. | Verified |
| BUG-20260506-023 | Backend/Security | Async/concurrency/lifecycle hazards | Medium | Confirmed | `SearchService` called `TrafficObserver.OnSearchResultsAsync()` with `_ =` inside a synchronous `try/catch`, so exceptions thrown after the first await were not observed by the search finalizer. | VirtualSoulfind traffic capture failures could surface as unobserved background task faults and skip remaining response observations. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~SearchServiceLifecycleTests` verifies observer failures are awaited, logged, and do not stop later responses. | Verified |
| BUG-20260506-024 | Backend/Security | Filesystem/path containment | High | Confirmed | `ContentLocator` fallback path-content lookup recursively enumerated configured local roots with `SearchOption.AllDirectories` after validating only the starting root. | A symlink or junction under Downloads/Incomplete could make fallback streaming lookup scan outside the intended allowed roots. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~ContentLocatorTests` and `scripts/check-path-containment.sh` verify fallback enumeration skips reparse points. | Verified |
| BUG-20260506-025 | Backend/Security | Filesystem/path containment | High | Confirmed | `LibraryHealthService` recursive scans used default directory recursion for user-selected library paths and summary enumeration. | A symlink or junction under an allowed library root could expand health scans outside the selected library tree. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~LibraryHealthServiceTests` and `scripts/check-path-containment.sh` verify library-health enumeration skips reparse points. | Verified |
| BUG-20260506-026 | Tests/Tooling | Weak or misleading test coverage | Medium | Confirmed | Full solution validation found `SecurityRoutesIntegrationTests.SecurityDashboard_VersionedRoute_ShouldSucceed` still using the shared integration fixture's `ReadWrite`-only identity after `SecurityController` was hardened to administrator-only. | Integration route smoke tests could fail for the wrong reason or drift away from the intended admin-only contract. | tests | `dotnet test tests/slskd.Tests.Integration/slskd.Tests.Integration.csproj --no-restore --filter FullyQualifiedName~SecurityRoutesIntegrationTests` verifies the shared integration fixture can exercise admin-only security routes. | Verified |
| BUG-20260506-027 | Backend/Security | Async/concurrency/lifecycle hazards | High | Confirmed | The async/lifecycle section found unobserved side-effect calls in `Application`, `SignalBus`, `SharesController`, `PodMessaging`, and `DownloadService`: notifications, SignalR broadcasts, relay sync/start, room rejoin, share rescans, pod routing, FTP upload, relay download completion, and peer metrics were assigned to `_ =` directly. | Failures thrown after the first await could become unobserved task exceptions, drop telemetry, or hide failed side-effect work during core messaging/transfer workflows. | backend/tests | `scripts/check-async-task-observation.sh` rejects the accepted raw patterns, and focused unit validation compiles the touched Application/SignalBus/Shares/Pod/Download paths. | Verified |
| BUG-20260506-028 | Backend/Security | Async/concurrency/lifecycle hazards | Medium | Confirmed | `src/slskd/VirtualSoulfind/DisasterMode/DisasterModeRecovery.cs` and `src/slskd/VirtualSoulfind/DisasterMode/DisasterModeCoordinator.cs` both used `async void` health callbacks. | A fault in recovery/escalation recovery work (`AttemptRecoveryAsync` / fallback escalation) could be surfaced as an unobserved exception, skipping health-driven state transitions silently. | backend | `scripts/check-async-void-handlers.sh` must remain clean; add focused tests if health monitor fixtures expose callback failures. | Verified |

| BUG-20260506-029 | Backend/Security | Mutable input arrays & shared ownership | Medium | Confirmed | `src/slskd/Common/Security/CanaryTraps.cs` stored caller-supplied secret key bytes by reference in `_secretKey`. | A caller can mutate the secret key array after construction and affect future HMAC canary generation. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~CanaryTrapsTests` verifies the internal `_secretKey` is not a shared reference. | Verified |
| BUG-20260506-030 | Backend/Security | Mutable input arrays & shared ownership | Medium | Confirmed | `src/slskd/Common/Security/ContentSafety.cs` stored protocol signature `magic` bytes by reference in `FileSignature`. | A caller can mutate signature bytes after constructor call and shift signature matching behavior. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~ContentSafetyTests.FileSignature_StoresCopyOfMagicBytes` verifies signature bytes are copied at construction. | Verified |
| BUG-20260506-031 | Backend/Security | Mutable input arrays & shared ownership | Medium | Confirmed | `src/slskd/Common/Security/IMessageBatcher.cs` stored queued message `data` by reference in `BatchedMessage`. | A caller can mutate queued message payload bytes between enqueue and batch flush. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~TimedBatcherTests.BatchedMessage_CopiesMessageData` verifies message payload bytes are copied. | Verified |
| BUG-20260506-032 | Frontend/Workflow | Primitive JSON body mismatches | Medium | Confirmed | `src/slskd/Events/API/EventsController.cs:123` receives `[FromBody] string disambiguator`; `src/web/src/lib/events.js` lacked `JSON.stringify(disambiguator)` usage before this cycle. | Event raise requests could miss ASP.NET model binding for primitive string bodies and fail with 400 or ignored parameters. | web | `scripts/check-web-json-string-bodies.sh`; `cd src/web && npm test -- events.test.js` | Verified |
| BUG-20260506-033 | Frontend/Workflow | Primitive JSON body mismatches | Medium | Confirmed | `src/slskd/Messaging/API/Controllers/RoomsController.cs:170` accepted `[FromBody] string message` for ticker updates; `src/web/src/lib/rooms.js` lacked serialized body for `.setTicker()`. | Ticker update calls could fail model binding for primitive payloads and silently drop room ticker actions. | web | `scripts/check-web-json-string-bodies.sh`; `cd src/web && npm test -- rooms.test.js` | Verified |
| BUG-20260506-034 | Frontend/Workflow | Primitive JSON body mismatches | Medium | Confirmed | `src/slskd/Messaging/API/Controllers/RoomsController.cs:211` accepted `[FromBody] string username` for room member add; `src/web/src/lib/rooms.js` lacked serialized body for `.addRoomMember()`. | Add-member calls could pass malformed request bodies and fail route execution. | web | `scripts/check-web-json-string-bodies.sh`; `cd src/web && npm test -- rooms.test.js` | Verified |
| BUG-20260506-035 | Backend/Security | Mutable input arrays & shared ownership | Medium | Confirmed | `src/slskd/Common/Security/IMessageBatcher.cs` stored nested dictionary/list metadata entries by reference; a caller could mutate nested metadata values after enqueue and alter batched metadata during transit. | Nested mutable metadata could alter routing or batch-processing decisions after queueing. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~TimedBatcherTests.BatchedMessage_DeepCopiesNestedMetadata` | Verified |
| BUG-20260506-036 | Backend/Security | Mutable input arrays & shared ownership | Medium | Confirmed | `src/slskd/Mesh/Dht/KademliaRpcClient.cs` `DhtStoreMessage.CreateSigned` stored caller-supplied `key`, `value`, and `requesterId` byte arrays by reference; a caller could mutate request arrays after signing and alter stored DHT store payloads. | Mutable signed store payloads could make DHT store operations sign inconsistent data and weaken protocol replay/verification reasoning. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~KademliaRpcClientTests.CreateSigned_CopiesMutableInputs` | Verified |
| BUG-20260506-037 | Backend/Security | Mutable input arrays & shared ownership | Medium | Confirmed | `src/slskd/Mesh/MeshPeer.cs` copied only the endpoint list container and exposed mutable `IPEndPoint` objects through `Addresses` and `GetBestAddress()`. | Caller-side mutation after peer discovery/update could silently rewrite mesh peer addresses used by circuit selection and diagnostics. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~MeshPeerTests` verifies constructor/update/accessor endpoint copies. | Verified |
| BUG-20260506-038 | Backend/Security | Protocol trust/signature fail-open | High | Confirmed | `DhtStoreMessage.CreateSigned()` tried to sign a temporary `MeshAckMessage` by reflecting into its read-only `Type` property; focused validation threw `System.ArgumentException: Property set method not found`. | Mesh DHT signed STORE operations could fail before remote store propagation, and the signing payload did not have a concrete DHT store message contract. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~KademliaRpcClientTests.CreateSigned"` verifies mutable input copies and real signer/verification roundtrip. | Verified |
| BUG-20260506-039 | Backend/Security | Mutable input arrays & shared ownership | Medium | Confirmed | `ContentVerificationResult.BestSources` and `BestSemanticSources` returned mutable backing lists from `SourcesByHash` / `SourcesBySemanticKey`. | A caller that clears or reorders the helper return could corrupt verification result buckets used by multi-source downloads, library-health remediation, and API test/download DTO wiring. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~ContentVerificationServiceTests"` verifies helper returns are snapshots. | Verified |
| BUG-20260506-040 | Backend/Security | Mutable input arrays & shared ownership | Medium | Confirmed | `TransportPolicy.GetEffectivePreferenceOrder()` returned either `TransportPreferenceOrder` or the caller-provided global order list directly. | Callers that sort, clear, or normalize the effective order could mutate policy/global transport option state and affect later mesh transport selection. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~TransportPolicyTests"` verifies custom and fallback effective orders are snapshots. | Verified |
| BUG-20260506-041 | Backend/Security | Secret/log/error leakage | High | Confirmed | `src/slskd/Relay/RelayService.cs` logged `Supplied credential {Credential}` and `expected credential {Expected}` when relay authentication challenge validation failed. | Debug logs could capture relay HMAC response material for both attacker-supplied and expected credentials, increasing credential replay/forensics exposure after log access. | backend | `scripts/check-sensitive-placeholders.sh`; `npm run check:remediation` reject raw credential/expected credential log placeholders. | Verified |
| BUG-20260506-042 | Backend/Security | Secret/log/error leakage | Medium | Confirmed | `SpotifyConnectionController.Authorize()` and callback completion returned `BadRequest(ex.Message)` from `InvalidOperationException`. | Spotify configuration/state failures could expose service exception text through API responses, including operational details intended for logs. | backend | `scripts/check-api-exception-bodies.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~SpotifyConnectionControllerTests` verifies stable response bodies. | Verified |
| BUG-20260506-043 | Backend/Security | Secret/log/error leakage | Medium | Confirmed | `SpotifyConnectionController.Callback()` reflected the OAuth provider `error` query value into callback HTML. | A provider or caller-controlled error string could be reflected into the browser-visible callback page, even though HTML encoding prevented script execution. | backend | `scripts/check-api-exception-bodies.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~SpotifyConnectionControllerTests` verifies callback HTML does not include raw provider error detail. | Verified |
| BUG-20260506-044 | Backend/Security | Secret/log/error leakage | Medium | Confirmed | `SongIdController.CreateRun()` returned `BadRequest(ex.Message)` for `ArgumentException` and `InvalidOperationException` from analysis queueing. | Local path validation, outbound guard reasons, or analyzer process stderr could be exposed directly to API clients. | backend | `scripts/check-api-exception-bodies.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~SongIdControllerTests` verifies stable response bodies. | Verified |
| BUG-20260506-045 | Backend/Security | Secret/log/error leakage | Medium | Confirmed | `ListeningPartyController.Publish()` returned `BadRequest(ex.Message)` for service `ArgumentException` values. | Listen-along validation details could expose content IDs, local paths, or service internals through API responses. | backend | `scripts/check-api-exception-bodies.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~ListeningPartyControllerTests` verifies stable response bodies. | Verified |
| BUG-20260506-046 | Release/Ops | Workflow semantic drift across manual vs automated paths | High | Confirmed | `.github/workflows/upstream-sync.yml` ran on a six-hour schedule, merged `upstream/master`, and pushed to `origin master` in a `main` fork. | Scheduled automation could push unreviewed upstream merge commits into the fork or fail against the wrong branch, bypassing the manual review posture for upstream sync work. | packaging | `scripts/check-workflow-main-branch-targets.sh`; `npm run check:remediation` verify upstream sync is manual-only and fork writes target `main`. | Verified |
| BUG-20260506-047 | Release/Ops | Workflow semantic drift across manual vs automated paths | Medium | Confirmed | `.github/workflows/check-upstream-access.yml` created an automated PR with `--base "master"` and only `contents: write` permission despite creating PRs and issues. | If upstream access changed, the notification workflow could fail to create its PR/issue or target the wrong base branch, leaving stale repo state and noisy scheduled failures. | packaging | `scripts/check-workflow-main-branch-targets.sh`; `scripts/check-workflow-yaml-syntax.sh` verify active branch targets and workflow shape. | Verified |
| BUG-20260506-048 | Frontend/Workflow | UI action wiring/state persistence bugs | Medium | Confirmed | `TransferGroup.getSelectedFiles()` parsed serialized bulk selections and dereferenced matching directories/files without guarding malformed selection strings, stale selections, or non-array directory payloads. | A refreshed Transfers payload or stale/corrupt selection could crash the Transfers page before retry/cancel/remove bulk actions render. | web | `cd src/web && npm test -- src/components/Transfers/TransferGroup.test.jsx` verifies malformed and stale selections are ignored. | Verified |
| BUG-20260506-049 | Frontend/Workflow | Workflow semantic drift across manual vs automated paths | Medium | Confirmed | `EditModal.save()` awaited YAML validation but then checked the previous `yamlError` React state value before updating. | Invalid options YAML could still be submitted when validation failed in the current save attempt but the prior rendered error state was empty. | web | `cd src/web && npm test -- src/components/System/Options/EditModal.test.jsx` verifies current validation errors block `updateYaml()`. | Verified |
| BUG-20260506-050 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `EditModal` stored `nextUpdateError.response.data` directly and concatenated it into a message, assuming every API error had a string response body. | Structured ProblemDetails responses rendered as `[object Object]`, and errors without `response` could throw inside the save error path. | web | `cd src/web && npm test -- src/components/System/Options/EditModal.test.jsx` verifies structured update errors render stable user text. | Verified |
| BUG-20260506-051 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `Contacts.loadContacts()` accepted any array entries and rendered `contact.peerId.slice(...)` without requiring contact objects with string peer ids. | A malformed Contacts API list item could crash the Contacts page during render. | web | `cd src/web && npm test -- src/components/Contacts/Contacts.test.jsx` verifies malformed contact entries are ignored. | Verified |
| BUG-20260506-052 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `Contacts.handleCreateInvite()` dereferenced `response.data.inviteLink` and `response.data.friendCode` directly. | A malformed invite response could throw inside the success path and leave the invite modal/error state inconsistent. | web | `cd src/web && npm test -- src/components/Contacts/Contacts.test.jsx` verifies malformed invite payloads render a stable error and do not generate a QR code. | Verified |
| BUG-20260506-053 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `LibraryHealth.handleStartScan()` dereferenced `response.data.scanId` and poll status bodies directly. | A malformed scan creation response could start polling with an undefined scan id or keep scanning against invalid status payloads. | web | `cd src/web && npm test -- src/components/System/LibraryHealth/index.test.jsx` verifies malformed scan creation responses do not poll. | Verified |
| BUG-20260506-054 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `SharedWithMe.handleBackfill()` dereferenced `result.data.failed/message` directly and could store structured error objects in `ErrorSegment`. | Malformed backfill responses or ProblemDetails errors could crash the incoming share modal or render unreadable object errors. | web | `cd src/web && npm test -- src/components/Shares/SharedWithMe.test.jsx` verifies malformed backfill responses normalize to zero counts and structured errors render as text. | Verified |
| BUG-20260506-055 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `Collections.handleDeleteCollection()` and `handleCreateShare()` stored raw `error.response.data` for render through `ErrorSegment`. | Structured collection/share ProblemDetails errors could render unreadable object text or throw React child errors. | web | `cd src/web && npm test -- src/components/Collections/Collections.test.jsx` verifies structured delete/share errors render as text. | Verified |
| BUG-20260506-056 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `ShareGroups.handleDeleteGroup()` and `handleRemoveMember()` stored raw `error.response.data` for render through `ErrorSegment`. | Structured share-group mutation errors could render unreadable object text or throw React child errors. | web | `cd src/web && npm test -- src/components/ShareGroups/ShareGroups.test.jsx` verifies structured delete errors render as text; the same helper covers remove-member mutation errors. | Verified |
| BUG-20260506-057 | Frontend/Workflow | UI action wiring/state persistence bugs | Low | Confirmed | `SharedWithMe` stored raw manifest/load error bodies and passed `error="Failed to load manifest"` to `ErrorSegment` instead of `caption`. | Incoming share manifest failures could show only an empty `Error:` prefix or crash on structured ProblemDetails bodies. | web | `cd src/web && npm test -- src/components/Shares/SharedWithMe.test.jsx` verifies structured manifest errors render as text. | Verified |
| BUG-20260506-058 | Backend/Security | Atomic security-state persistence | High | Confirmed | `FileKeyStore.WriteToFile()` wrote `mesh-overlay.key` directly before chmod. Fixed with sibling temp file, flushed write, restrictive temp mode, atomic replacement, and temp cleanup. | An interrupted overlay-key save could corrupt the Ed25519 identity file and force identity rotation or startup failure. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~FileKeyStoreTests` verifies key reload and no temp-file residue. | Verified |
| BUG-20260506-059 | Backend/Security | Atomic security-state persistence | High | Confirmed | `CertificateManager.SaveCertificate()` wrote `overlay_cert.pfx` directly. Fixed with flushed sibling temp file replacement before legacy password cleanup. | An interrupted overlay certificate save could corrupt the private-key PFX and reset overlay TLS identity. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~CertificateManagerTests` verifies valid reload and no temp-file residue. | Verified |
| BUG-20260506-060 | Backend/Security | Atomic security-state persistence | Medium | Confirmed | `CertificatePinManager.PersistPins()` overwrote `certificate-pins.json` directly. Fixed with flushed sibling temp file replacement and restrictive temp mode. | An interrupted mesh pin save could corrupt the TOFU pin store and weaken peer trust continuity after restart. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~CertificatePinManagerTests` verifies persisted pin reload and no temp-file residue. | Verified |
| BUG-20260506-061 | Backend/Security | Atomic durable-state persistence | High | Confirmed | `ProfileService` wrote `peer-profile.key` and `peer-profile.json` directly. Fixed by using `AtomicFileWriter` for key/profile saves, including restrictive temp mode for the private key. | Interrupted peer profile or private-key saves could corrupt local social identity state and force profile/key regeneration. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~AtomicFileWriterTests|FullyQualifiedName~ProfileServiceTests"` verifies atomic helper behavior and existing profile/key flows. | Verified |
| BUG-20260506-062 | Backend/Security | Atomic durable-state persistence | Medium | Confirmed | `PeerReputationStore.SaveToDiskAsync()` wrote encrypted reputation bytes directly to `_storagePath`. Fixed by replacing direct writes with `AtomicFileWriter.WriteAllBytesAsync()`. | Interrupted encrypted reputation saves could truncate peer-moderation history and weaken later trust/ranking decisions. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~AtomicFileWriterTests|FullyQualifiedName~PeerReputationStoreTests"` verifies atomic helper behavior and reputation persistence. | Verified |
| BUG-20260506-063 | Network Health | Atomic durable-state persistence | Medium | Confirmed | `DhtRendezvousService.StopAsync()` wrote `dht_nodes.bin` directly during shutdown. Fixed by atomically replacing the saved DHT node table. | Interrupted shutdown could leave a corrupt node table, slowing restart bootstrap and increasing unnecessary DHT bootstrap traffic. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~AtomicFileWriterTests|FullyQualifiedName~DhtRendezvousServiceTests"` verifies atomic helper behavior and DHT service coverage. | Verified |
| BUG-20260506-064 | Backend/Security | Atomic durable-state persistence | Low | Confirmed | `AutoReplaceBackgroundService.SaveState()` wrote `auto-replace-state.json` directly. Fixed by using `AtomicFileWriter.WriteAllText()`. | Interrupted preference saves could lose the operator's manual auto-replace enable/disable state. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~AtomicFileWriterTests` verifies atomic replacement semantics used by the state writer. | Verified |
| BUG-20260506-065 | Network Health | Atomic durable-state persistence | Medium | Confirmed | `ContentVerificationService.SaveProbeBudget()` wrote the per-peer probe-budget JSON directly. Fixed by using `AtomicFileWriter.WriteAllText()`. | Interrupted probe-budget saves could reset persisted per-peer probe counts after restart and allow extra verification probes against Soulseek peers. | backend | `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~AtomicFileWriterTests|FullyQualifiedName~ContentVerificationServiceTests"` verifies atomic helper behavior and verification-probe budget coverage. | Verified |
| BUG-20260506-066 | Network Health | Async/concurrency/lifecycle hazards | Medium | Confirmed | `MeshOverlayServer.AcceptLoopAsync()` detached `HandleConnectionAsync()` directly. Fixed by wrapping inbound connection tasks in `TaskObservation.Observe()` and adding a scanner guard. | Faults after connection acceptance could become unobserved task failures instead of being logged against the overlay server lifecycle. | backend | `scripts/check-async-task-observation.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~MeshOverlayServerTests` verifies the scanner and existing server lifecycle coverage. | Verified |
| BUG-20260506-067 | Network Health | Async/concurrency/lifecycle hazards | Medium | Confirmed | `MeshOverlayServer.HandleConnectionAsync()` detached `HandleMessagesAsync()` directly after registering an inbound session. Fixed by observing the inbound message loop task. | Cleanup/unregister/dispose faults in an inbound mesh session loop could be dropped, hiding connection lifecycle failures. | backend | `scripts/check-async-task-observation.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~MeshOverlayServerTests` verifies the scanner and existing server lifecycle coverage. | Verified |
| BUG-20260506-068 | Network Health | Async/concurrency/lifecycle hazards | Medium | Confirmed | `MeshOverlayConnector.ConnectToEndpointAsync()` detached `RunOutboundMessageLoopAsync()` directly after registering an outbound session. Fixed by observing the outbound message loop task. | Cleanup/unregister/dispose faults in outbound mesh sessions could be unobserved, hiding overlay connection instability. | backend | `scripts/check-async-task-observation.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~MeshOverlayConnectorTests` verifies the scanner and existing connector lifecycle coverage. | Verified |
| BUG-20260506-069 | Tests/Tooling | Atomic durable-state persistence | Low | Confirmed | `JobManifestService.ExportAsync()` wrote YAML manifests with direct `File.WriteAllTextAsync()`. Fixed by using `AtomicFileWriter.WriteAllTextAsync()` and scanner coverage. | Interrupted manifest exports could leave truncated job YAML and break later import/replay of that job. | tests | `scripts/check-durable-state-atomic-writes.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~AtomicFileWriterTests|FullyQualifiedName~JobManifestValidatorTests"` verifies the writer and adjacent manifest validation. | Verified |
| BUG-20260506-070 | Backend/Security | Atomic durable-state persistence | Medium | Confirmed | `QuarantineJuryService.PersistState()` used a fixed `.tmp` temp path with unflushed writes. Fixed by using `AtomicFileWriter.WriteAllText()`. | Interrupted or concurrent jury-state saves could leave stale temp files or partially persisted quarantine review decisions. | backend | `scripts/check-durable-state-atomic-writes.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~QuarantineJuryServiceTests` verifies jury persistence flows. | Verified |
| BUG-20260506-071 | Backend/Security | Atomic durable-state persistence | High | Confirmed | `SpotifyConnectionService.PersistState()` wrote protected token state through a fixed `.tmp` temp path with no flush or restrictive temp mode. Fixed by using `AtomicFileWriter.WriteAllText()` with user-only Unix mode. | Interrupted token saves could corrupt Spotify connection state or expose protected token material through less restrictive temp-file permissions. | backend | `scripts/check-durable-state-atomic-writes.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~SpotifyConnectionControllerTests` plus remediation scanner cover the fixed token writer. | Verified |
| BUG-20260506-072 | Backend/Security | Atomic durable-state persistence | Low | Confirmed | `SourceFeedImportService.PersistHistory()` used a fixed `.tmp` temp path with unflushed writes. Fixed by using `AtomicFileWriter.WriteAllText()`. | Interrupted source-feed import history saves could lose or corrupt recent import context used by follow-up workflow suggestions. | backend | `scripts/check-durable-state-atomic-writes.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~SourceFeedImportServiceTests` verifies import history flows. | Verified |
| BUG-20260506-073 | Backend/Security | Atomic durable-state persistence | Medium | Confirmed | `ArtistReleaseRadarService.PersistState()` used a fixed `.tmp` temp path with unflushed writes. Fixed by using `AtomicFileWriter.WriteAllText()`. | Interrupted radar-state saves could lose subscription, notification, or route-attempt state and repeat or drop MusicBrainz notifications. | backend | `scripts/check-durable-state-atomic-writes.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~ArtistReleaseRadarServiceTests` verifies radar persistence flows. | Verified |
| BUG-20260506-074 | Backend/Security | Atomic durable-state persistence | Medium | Confirmed | `MusicBrainzOverlayService.PersistState()` used a fixed `.tmp` temp path with unflushed writes. Fixed by using `AtomicFileWriter.WriteAllText()`. | Interrupted overlay-state saves could corrupt pending edits, export decisions, or routing attempts. | backend | `scripts/check-durable-state-atomic-writes.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~MusicBrainzOverlayServiceTests` verifies overlay persistence flows. | Verified |
| BUG-20260506-075 | Backend/Security | Atomic durable-state persistence | Medium | Confirmed | `RealmSubjectIndexService.PersistState()` used a fixed `.tmp` temp path with unflushed writes. Fixed by using `AtomicFileWriter.WriteAllText()`. | Interrupted subject-index saves could corrupt realm index/proposal/authority decision state. | backend | `scripts/check-durable-state-atomic-writes.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~RealmSubjectIndexServiceTests` verifies subject-index persistence flows. | Verified |
| BUG-20260506-076 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `ExperienceSettings.readStoredPreferences()` merged any object-shaped localStorage payload over defaults without validating enum, boolean, or numeric-string fields. | Corrupt or manually edited browser-local preferences could put Semantic UI selects/checkboxes and copied reports into invalid states that page-specific consumers might later misread. | web | `cd src/web && npm test -- src/components/System/ExperienceSettings/index.test.jsx` verifies malformed persisted field values fall back to defaults while valid values survive. | Verified |
| BUG-20260506-077 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | `UserNoteModal` dereferenced `response.data.note`, `color`, and `isHighPriority` after checking only that `response.data` was truthy. | A malformed user-note API body could render invalid color/high-priority state or throw while opening the note modal. | web | `cd src/web && npm test -- src/components/Users/UserNoteModal.test.jsx` verifies malformed/null note responses normalize to safe defaults. | Verified |
| BUG-20260506-078 | Frontend/Workflow | Web API helper contract drift | Medium | Confirmed | `MusicBrainzLookup.handleLookup()` treated any truthy target response as success and enabled graph actions even when no `album` or `track` object was present. | A malformed MusicBrainz target response could display a false success toast and enable graph actions with no valid target identifiers. | web | `cd src/web && npm test -- src/components/Search/MusicBrainzLookup.test.jsx` verifies malformed target responses surface a stable error and leave graph actions disabled. | Verified |
| BUG-20260506-079 | Frontend/Workflow | Async/concurrency/lifecycle hazards | Medium | Confirmed | `LibraryHealth.handleStartScan()` created a scan polling interval plus 60-second timeout without component-owned cleanup, and poll callback failures were not handled. | Leaving the page during a scan could leave stale polling/summary refreshes active; a status-poll failure could leave the UI stuck in scanning state. | web | `cd src/web && npm test -- src/components/System/LibraryHealth/index.test.jsx` verifies unmount cleanup and poll-failure handling. | Verified |
| BUG-20260506-080 | Frontend/Workflow | Async/concurrency/lifecycle hazards | Low | Confirmed | `System/Shares` scheduled a delayed post-scan `getAll(true)` refresh without clearing it on unmount or scan-state replacement. | Navigating away during share scan state transitions could trigger stale refresh calls and state updates after the Shares page is gone. | web | `cd src/web && npm test -- src/components/System/Shares/index.test.jsx` verifies delayed post-scan refreshes are canceled on unmount. | Verified |
| BUG-20260506-081 | Frontend/Workflow | Web API helper contract drift | Low | Confirmed | Search hub `create`, `update`, and `delete` handlers stored hub payloads under `search.id` without requiring an object with a string id. | Malformed hub mutation events could create an `undefined` search entry or delete/update the wrong transient state key. | web | `cd src/web && npm test -- src/components/Search/Searches.test.jsx` verifies malformed hub mutation events are ignored. | Verified |
| BUG-20260506-082 | Release/Ops | Release/tag/package/version drift | High | Confirmed | `packaging/proxmox-lxc/setup-inside-ct.sh` downloaded release ZIP assets without downloading or checking `SHA256SUMS.txt`. | A raw Proxmox install path could install an unverified release artifact even though the general Linux installer refuses missing or mismatched checksums. | packaging | `scripts/check-linux-installer-safety.sh`; `bash -n packaging/proxmox-lxc/setup-inside-ct.sh` verify checksum enforcement is present. | Verified |
| BUG-20260506-083 | Release/Ops | Installer/systemd/container permission bugs | Medium | Confirmed | `packaging/proxmox-lxc/setup-inside-ct.sh` unzipped new releases into `${DEST}` without first removing the old install tree. | Upgrades could leave stale assemblies/assets behind and make a Proxmox container run a mixed old/new application tree. | packaging | `scripts/check-linux-installer-safety.sh` requires raw and Proxmox installers to remove `${DEST}` before extraction. | Verified |
| BUG-20260506-084 | Release/Ops | Installer/systemd/container permission bugs | Medium | Confirmed | The Proxmox LXC installer did not converge service permissions with the raw Linux installer: no `UMask=0002`, no config `chmod 664`, no app-tree ownership, and no group-writable data dir normalization. | Proxmox installs could diverge from package/raw-install behavior, causing config edits or service runtime writes to fail depending on local umask and extracted archive ownership. | packaging | `scripts/check-linux-installer-safety.sh`; `scripts/check-systemd-permission-matrix.sh` verify aligned installer safety and service permission posture. | Verified |
| BUG-20260506-085 | Network Health | Async/concurrency/lifecycle hazards | Medium | Confirmed | `QuicOverlayServer.HandleConnectionAsync()` detached each accepted control stream with `_ = HandleStreamAsync(...)` while shutdown drained only connection tasks. | Active QUIC control streams could continue past server shutdown ownership, hiding stream cleanup failures and leaving overlay lifecycle behavior harder to reason about. | backend | `scripts/check-async-task-observation.sh`; `dotnet test tests/slskd.Tests.Unit/slskd.Tests.Unit.csproj --no-restore --filter FullyQualifiedName~QuicOverlayServerTests`; `dotnet build src/slskd/slskd.csproj --no-restore` verify stream-task ownership compiles and the scanner rejects raw detached stream handlers. | Verified |
| BUG-20260506-086 | Network Health | Async/concurrency/lifecycle hazards | Medium | Confirmed | `QuicDataServer.HandleConnectionAsync()` detached each accepted data stream with `_ = HandleStreamAsync(...)` while shutdown drained only connection tasks. | Active QUIC data streams could outlive the data server's shutdown drain, risking stale relay/bulk-transfer stream work after overlay stop. | backend | `scripts/check-async-task-observation.sh`; `dotnet build src/slskd/slskd.csproj --no-restore` verify data-stream task ownership compiles and the scanner rejects raw detached stream handlers. | Verified |
| BUG-20260506-087 | Release/Ops | Config/docs/example drift | Medium | Confirmed | `packaging/unraid/slskdn.xml` advertised this fork's Unraid template through `raw.githubusercontent.com/snapetech/slskdn/master/...` while the fork's active branch is `main`. | Unraid template refresh/install paths could follow a stale or missing branch URL and fail to fetch updated slskdN container metadata. | packaging | `scripts/check-fork-main-branch-links.sh`; `npm run check:remediation` verify active fork package links target `main`. | Verified |
| BUG-20260506-088 | Release/Ops | Config/docs/example drift | Low | Confirmed | `packaging/chocolatey/slskdn.nuspec` linked the package license to `github.com/snapetech/slskdn/blob/master/LICENSE` while the fork's active branch is `main`. | Chocolatey package metadata could expose a stale license URL, making package review/install metadata less reliable. | packaging | `scripts/check-fork-main-branch-links.sh`; `npm run check:remediation` verify active fork package links target `main`. | Verified |
| BUG-20260506-089 | Docs/Config | Config/docs/example drift | Low | Confirmed | `docs/dev/e2e-testing-guide.md` still showed an active GitHub Actions E2E example using `pull_request.branches: [master]`. | Contributors could copy a stale branch target and miss `main` pull-request E2E coverage. | docs | `scripts/check-fork-main-branch-links.sh`; `npm run check:remediation` verify active GitHub Actions examples target `main`. | Verified |
| BUG-20260506-090 | Release/Ops | Config/docs/example drift | Low | Confirmed | `.github/workflows/ci.yml` and `.github/workflows/codeql.yml` still described active fork scans/build policy as `master` behavior in comments. | Maintainers reading workflow policy could misinterpret the active branch and reintroduce branch-push build or CodeQL scope drift. | packaging | `scripts/check-fork-main-branch-links.sh`; `npm run check:remediation` verify active workflow comments describe `main`. | Verified |
| BUG-20260506-091 | Docs/Config | Config/docs/example drift | Low | Confirmed | `docs/FEATURES.md` and active `docs/security/*.md` files still instructed `git checkout master` or labeled slskdN branch state as `master`. | Contributors following active fork docs could check out a stale/missing branch or misread current security documentation as applying to an obsolete branch. | docs | `scripts/check-fork-main-branch-links.sh`; `npm run check:remediation` verify active fork docs describe `main` while upstream `slskd/slskd` references remain allowed. | Verified |
| BUG-20260506-092 | Tests/Tooling | Weak or misleading test coverage | Medium | Confirmed | The updated negative-space council inventory declared six trust boundaries, but `scripts/check-council-negative-space.sh` enforced only path, outbound HTTP, and anonymous-endpoint representatives; it also documented controller sinks as non-existent `src/slskd.Web/Controllers`. | The broader council gate could pass after removing validators for mutating API roles/CSRF, durable state atomic writes, or runtime-crossing task observation, giving false confidence that every declared boundary remained protected. | tests | `scripts/check-council-negative-space.sh`; `npm run check:remediation`; `git diff --check` verify every declared boundary has a concrete validator and remediation-baseline registration. | Verified |
| BUG-20260506-093 | Frontend/Workflow | Async/concurrency/lifecycle hazards | Medium | Confirmed | `src/web/src/components/System/Network/index.jsx` refreshed System Network data every 5 seconds, but in-flight API calls could resolve after the component unmounted and still update state. | Navigating away from System Network during slow stats/peer calls could produce stale post-unmount updates and noisy lifecycle behavior in the admin UI. | web | `scripts/check-web-polling-lifecycle.sh`; `npm run check:remediation` verify System Network polling has mounted-ref async completion guards. | Verified |
| BUG-20260506-094 | Frontend/Workflow | Async/concurrency/lifecycle hazards | Medium | Confirmed | `src/web/src/components/System/Mesh/index.jsx` refreshed mesh stats and rendezvous status without guarding post-await state updates after unmount. | Leaving the Mesh dashboard during slow stats/rendezvous requests could update stale component state after route changes. | web | `scripts/check-web-polling-lifecycle.sh`; `npm run check:remediation` verify System Mesh polling has mounted-ref async completion guards. | Verified |
| BUG-20260506-095 | Frontend/Workflow | Async/concurrency/lifecycle hazards | Medium | Confirmed | `src/web/src/components/System/SwarmVisualization/index.jsx` refreshed swarm status every 2 seconds with no mounted guard around the `Promise.allSettled` completion. | Switching away from a selected swarm job during slow status/trace calls could leave stale async updates racing route changes. | web | `scripts/check-web-polling-lifecycle.sh`; `cd src/web && npm test -- src/components/System/SwarmVisualization/index.test.jsx` verify guarded swarm visualization polling. | Verified |
| BUG-20260506-096 | Frontend/Workflow | Async/concurrency/lifecycle hazards | Medium | Confirmed | `src/web/src/components/System/SwarmAnalytics/index.jsx` refreshed analytics every 30 seconds and updated five state slices after awaited API calls without unmount guards. | Leaving or reconfiguring the analytics view during slow calls could update stale state after the component lifecycle ended. | web | `scripts/check-web-polling-lifecycle.sh`; `cd src/web && npm test -- src/components/System/SwarmAnalytics/index.test.jsx` verify guarded analytics polling. | Verified |
| BUG-20260506-097 | Frontend/Workflow | Async/concurrency/lifecycle hazards | Medium | Confirmed | `src/web/src/components/System/Jobs/index.jsx` polled active swarm jobs and loaded paged jobs without guarding async completions after unmount. | System Jobs could set job/swarm/loading state after navigation away from the dashboard. | web | `scripts/check-web-polling-lifecycle.sh`; `cd src/web && npm test -- src/components/System/Jobs/index.test.jsx` verify guarded Jobs polling. | Verified |
| BUG-20260506-098 | Frontend/Workflow | Async/concurrency/lifecycle hazards | Medium | Confirmed | `src/web/src/components/System/Security/index.jsx` refreshed the security dashboard every 30 seconds but updated dashboard/loading/refresh state after awaited calls without an unmount guard. | Leaving the Security tab during slow dashboard calls could update stale state after route changes. | web | `scripts/check-web-polling-lifecycle.sh`; `npm run check:remediation` verify guarded Security polling. | Verified |
| BUG-20260506-099 | Frontend/Workflow | Async/concurrency/lifecycle hazards | Medium | Confirmed | `src/web/src/components/System/Bridge/index.jsx` loaded bridge config/dashboard and refreshed the dashboard every 10 seconds without guarding async completions after unmount. | Navigating away from Bridge during slow config/dashboard calls could update stale bridge state after the component was gone. | web | `scripts/check-web-polling-lifecycle.sh`; `npm run check:remediation` verify guarded Bridge polling. | Verified |
| BUG-20260506-100 | Frontend/Workflow | Async/concurrency/lifecycle hazards | Medium | Confirmed | `src/web/src/components/System/MediaCore/index.jsx` refreshed content-id stats every 60 seconds without guarding post-await state updates after unmount. | Leaving the large MediaCore admin page during slow stats refreshes could update stale state after navigation away. | web | `scripts/check-web-polling-lifecycle.sh`; `cd src/web && npm test -- src/components/System/MediaCore/index.test.jsx` verify guarded MediaCore polling still renders. | Verified |

## Expert Council Notes

Use five read-only review roles for broad sweeps before fixing:

- Backend/Security: controllers, auth, SSRF, path, async, protocol trust.
- Frontend/Workflow: routing, API helpers, buttons, persisted state, e2e journeys.
- Release/Ops: workflows, package metadata, installers, systemd/container behavior.
- Network Health: Soulseek search/browse/download/probe automation, limiter use, cancellation.
- Adversarial Reviewer: duplicate detection, false-positive pressure, repro demands.

Add expert findings as `New`, then move only validated rows to `Accepted`.

## 2026-05-06 Non-Runtime Section Classification

- Route-state user navigation: classified as existing guard; scanned hits carry `?user=` URL intent and use router state only as adjunct state.
- Primitive JSON bodies: accepted and verified `BUG-20260506-032`, `BUG-20260506-033`, and `BUG-20260506-034`; remaining `[FromBody]` string targets are covered by the helper tests and `scripts/check-web-json-string-bodies.sh`.
- Outbound HTTP guards: classified as existing guard for the inspected app callers; current no-redirect/DNS guard scanner remains green.
- Async/lifecycle: accepted `BUG-20260506-027` and `BUG-20260506-028`; remaining inspected fire-and-forget tasks catch their own exceptions, route through observation helpers, or remain internal loops that are already handled (`LibraryHealthService`, `MeshTransferService`, `BridgeProxyServer`, `LocalPortForwarder`).
- Filesystem/path containment: accepted `BUG-20260506-024` and `BUG-20260506-025`; remaining inspected writes were rooted in app/config paths or already guarded.
- Tests/tooling: accepted `BUG-20260506-026` after full solution validation caught integration fixture role drift from the previous security telemetry hardening.
- Second async/lifecycle pass: accepted `BUG-20260506-027`; remaining raw hits are classified as existing guard (`Observe*`/`PublishPodInBackgroundAsync`) or internally handled loops (`LibraryHealthService`, `MeshTransferService`, `BridgeProxyServer`, `LocalPortForwarder`).
- Mutable input arrays and shared ownership: accepted `BUG-20260506-029` through `BUG-20260506-031`, `BUG-20260506-035` through `BUG-20260506-037`, and `BUG-20260506-039` through `BUG-20260506-040`; remaining mutable nested payload metadata values, constructor collection candidates, and computed helper list candidates were reviewed in this section and classified as value-only, immediate-use, already copied, lower-risk public model state, or fixed snapshot boundaries.
- DHT protocol signatures: accepted `BUG-20260506-038` after focused mutable-ownership validation found `CreateSigned()` could not sign a DHT STORE payload through the mesh signer. The fix adds a concrete `DhtStore` mesh message type and verifies real signer roundtrips.
- Secret/log/error leakage: accepted `BUG-20260506-041` for raw relay authentication credential mismatch logging; the broader exception-message scan classified authenticated validation responses, intentionally hashed relay IDs, and existing query/filename operational logs as policy/backlog candidates rather than high-confidence secret leaks in this section.
- API exception response leakage: accepted `BUG-20260506-042` through `BUG-20260506-045`; remaining non-controller `ex.Message` assignments in transfer/direct transport status models were classified as internal status evidence rather than raw HTTP response bodies for this section.
- Release/ops workflow branch targets: accepted `BUG-20260506-046` and `BUG-20260506-047`; remaining release/package workflow hits were covered by existing asset, YAML, tag-trigger, runtime-matrix, and packaging metadata scanners or classified as archived `.orig`/docs history.
- Web/API contract and persisted-state drift: accepted `BUG-20260506-048` through `BUG-20260506-050`; guarded `App`, `Messaging`, `Visualizer`, Library Health list payloads, mesh/port-forwarding direct fetch helpers, and route-state adjunct links were classified as existing guard for this section.
- Web response-object shape drift: accepted `BUG-20260506-051` through `BUG-20260506-054`; remaining inspected direct property reads in Playlist Intake, Jobs/Bridge helpers, User Notes, and guarded search panels were classified as existing guard or explicit handled failure paths for this section.
- Web error-rendering object drift: accepted `BUG-20260506-055` through `BUG-20260506-057`; remaining inspected error-message helpers in Contacts, Collections create/add-item, ShareGroups create/add-member, Transfers, and options were classified as existing string-normalized guards for this section.
- App security-state persistence: accepted `BUG-20260506-058` through `BUG-20260506-060` for direct writes of overlay keys, overlay certificates, and mesh certificate pins; `DhtRendezvous.Security.CertificatePinStore.Save()` was classified as an existing atomic-write guard, and inspected outbound HTTP plus mutating-role hits remained covered by existing no-redirect/role scanners for this section.
- Durable app-state persistence: accepted `BUG-20260506-061` through `BUG-20260506-065` for direct writes of profile identity state, encrypted reputation data, DHT node state, auto-replace operator state, and verification probe budgets. Existing temp-move state writers in quarantine jury, source feeds, MusicBrainz, realm subject index, options/security config controllers, and DHT certificate pins were classified as lower-risk existing guards or future flush-hardening candidates.
- DHT overlay async lifecycle: accepted `BUG-20260506-066` through `BUG-20260506-068` for detached inbound connection, inbound message-loop, and outbound message-loop tasks. `DhtRendezvousService.TryConnectToPeerAsync`, `DhtPeerGreetingService.SendGreetingAsync`, `EventBus.InvokeSubscriberAsync`, `SignalBus.ObserveChannelStartAsync`, and tracked QUIC overlay/data connection tasks were classified as existing guard or tracked/drained task ownership in this section.
- Durable temp-move state writers: accepted `BUG-20260506-069` through `BUG-20260506-075` for direct or fixed-temp writes of job manifests, quarantine jury state, Spotify protected token state, source-feed import history, MusicBrainz radar/overlay state, and realm subject indexes. Existing app/config writers with dedicated backup semantics, browse cache replacement, test/report output, and runtime/vendor writers were left out of this non-runtime section.
- Web boundary object normalization: accepted `BUG-20260506-076` through `BUG-20260506-078` for field-level normalization of stored Experience preferences, user-note API bodies, and MusicBrainz target responses. Existing guarded localStorage readers, Web list guards, route-state URL intent, and search stream actions were classified as existing guard or lower-risk handled failure paths for this section.
- Web lifecycle and hub mutation events: accepted `BUG-20260506-079` through `BUG-20260506-081` for Library Health scan timer ownership, Shares delayed refresh cleanup, and Search hub mutation payload guards. Info/Options delayed YAML rendering, guarded interval effects, and hub listeners with active flags/stop cleanup were classified as existing guard or lower-risk display hydration for this section.
- Proxmox/raw Linux installer safety: accepted `BUG-20260506-082` through `BUG-20260506-084` for Proxmox release checksum verification, stale install-tree replacement, and permission posture drift from the raw Linux installer. Active release asset matrix, .NET runtime matrix, and systemd package permission checks passed after the scanner addition; archived workflow `.orig` files and docs/archive examples were classified as out of active release path.
- QUIC overlay/data stream lifecycle: accepted `BUG-20260506-085` and `BUG-20260506-086` for detached accepted stream tasks in the QUIC control-plane and data-plane servers. Existing connection-task tracking, DHT overlay observation, and other inspected fire-and-forget paths were classified as already observed, internally handled, or tracked by their owning service for this section.
- Active fork branch links and examples: accepted `BUG-20260506-087` through `BUG-20260506-089` for active Unraid, Chocolatey, and E2E guide references to this fork's stale `master` branch. Upstream `slskd/slskd` reference links, AUR's external `master` branch convention, archived docs, and `.orig` workflow history were classified as out of scope or intentionally external for this non-runtime section.
- Active fork branch wording: accepted `BUG-20260506-090` and `BUG-20260506-091` for stale active workflow comments and active fork docs still describing slskdN as `master`. Upstream baseline comparisons, third-party `actions/*@master` pins, music metadata uses of "master/remaster", historical changelog entries, and archived docs were classified as out of scope or intentionally external.
- New-council negative-space gate: accepted `BUG-20260506-092` for a false-negative gap in the new slskd negative-space gate. The phase tracker/meta-runner wiring was incorporated, controller sink paths were corrected to the actual `src/slskd` API layout, and every declared boundary now asserts both its validator symbol/file and the detailed check script's remediation-baseline registration.
- Web System polling lifecycle: accepted `BUG-20260506-093` through `BUG-20260506-100` for Network, Mesh, Swarm Visualization, Swarm Analytics, Jobs, Security, Bridge, and MediaCore pollers that cleared intervals but did not guard in-flight async completions after unmount. Library Health, Transfers, Shares, and guarded one-shot effects were classified as already owning timer/active lifecycle or covered by earlier rows.
